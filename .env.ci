# Minimal env for CI — real secrets are injected via GitHub Actions environment variables.
APP_ENV=test
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/diving_planet_test
OPENAI_API_KEY=sk-test-not-real
CHATWOOT_BASE_URL=http://localhost:3300
CHATWOOT_API_TOKEN=ci-token
REDIS_URL=redis://localhost:6379/0
