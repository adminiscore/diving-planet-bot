# Session handoff

Read this file before changing code in the Diving Planet Bot. For a quick version overview, read `docs/HISTORY.md` first.

## Current branch and workflow

- **2026-07-16 (v0.20.11) — bug real "paquetes multidía" sin precio + red de seguridad en atajos deterministas**: reportado en vivo, "los paquetes multidía que precio tienen?" devolvía el resumen genérico de precios (2 buceos/minicurso/snorkel/OW) en vez de retrieval real con la info de multi-día. Causa raíz: `_PRICE_SPECIFIC` (`rag_agent.py`) solo tenía `paquete` en singular (no matcheaba plural) y ningún término de multi-día — arreglado (`paquetes?` + `multi-día`/`varios días`/`\d días|buceos|inmersiones`/`multi-day`, ES+EN). De ahí surgió una revisión más amplia con el equipo sobre el patrón de fondo: los atajos deterministas (regex de disparo amplio + exclusión que nunca puede ser exhaustiva) pueden dar una respuesta genérica en silencio ante frases no previstas. Se añadió una frase de cierre invitando a repreguntar con detalle en los 4 atajos canónicos del RAG (`_canonical_food_answer`, `_canonical_diving_overview_answer`, `_canonical_price_overview_answer`, más `_ambiguous_location_clarification` que ya preguntaba por diseño) y en 2 atajos deterministas de `supervisor.py` (`_maybe_answer_how_to_book_with_known_activity`, `_maybe_answer_age_eligibility`). Logging endurecido a un tag común grepable `[RAG][CANONICAL_SHORTCUT] shortcut=<nombre> query=<completa>`. **Pendiente de decisión del equipo** (no implementado): migrar `_canonical_price_overview_answer` de lista de exclusión a lista de permitidos — ver `docs/canonical-shortcuts-safelist-decision.md`. Se evaluó explícitamente automatizar el registro de estos eventos a un Google Sheet en tiempo real + revisión periódica mía — **decidido no implementar por ahora**. Suite: 1539 passed, 15 skipped. Ver `docs/HISTORY.md` v0.20.11.
- **2026-07-10 (v0.20.10) — link informativo en ambos resúmenes + bug real de reset en la vista previa**: (1) pedido por el owner, el resumen individual y el resumen del carrito ahora muestran el link informativo (`web_url`) del servicio — el link de RESERVA (`booking_url`, 10% online) sigue esperando a la pregunta de nacionalidad (colombianos pagan 50/50 vía asesor, no deben ver un link de reserva directa). (2) Bug real reportado en vivo: una pregunta de seguimiento en la vista previa final ("Vale y como reservo?") era mal clasificada por el orquestador LLM como "empezar reserva nueva", reseteando todo el flujo (plan/cantidad/última-inmersión perdidos). Arreglado con una guarda en `orchestrator_start_activity` (`decision_tree.py`): si ya se está a mitad de la MISMA actividad, un intento de reiniciarla se ignora y cae a RAG en vez de resetear. Suite: 1523 passed. Ver `docs/HISTORY.md` v0.20.10.
- **2026-07-10 (v0.20.9, feature/dev_alvaro) — bug PII en vivo: el bot recogía datos personales en el chat para "tramitar la reserva"** (nombres y apellidos + cédula/pasaporte + fechas, ritual del asesor humano improvisado por el LLM). Fix doble: guard determinista `requests_personal_data` (`grounding_check.py`, enganchado a la cadena de guards de `rag_answer` — verbo de petición + dato de identidad, o lista numerada) + regla explícita ES/EN en el prompt ("el cierre de reserva es SIEMPRE el link online o el asesor; los datos los recoge el asesor/formulario, nunca el bot"). Verificado 3/3 en el escenario exacto reportado. 14 tests nuevos en `test_rag_safety.py`. Suite: 1536 passed. Desplegado a PRE (`288a426`).
- **2026-07-09/10 (v0.20.8, feature/dev_alvaro) — umbral de RAG calibrado + agujero de alucinación cerrado**: el bug "pregunta general tras los links de reserva → fallback" NO era el cierre de reserva: PRE corría `RAG_MIN_SCORE=0.50` y el FAQ correcto puntúa coseno ~0.45. Calibración seria con set etiquetado (`scripts/calibrate_rag_threshold.py` + `docs/rag-eval-set.json`, 135 positivos sintéticos + 16 negativos; ver `docs/rag-threshold-calibration.md`): 0.50 → recall@gold 79.3%, 0.40 → 88.1%; 0.40 es el codo de la curva. **El umbral de PRE quedó fijado en `docker-compose.vps.yml`** (`environment:` pisa al `.env.pre` del VPS — versionado, sin SSH). De paso se cerró un agujero GRAVE oculto: con cero docs confiables, la vía "responde solo con contexto de estado" saltaba el juez de grounding en el camino del agente (`verify_grounding=False`) → el LLM respondía de su conocimiento del mundo (inventaba listas de peces); ahora esa vía fuerza el juez SIEMPRE (`require_grounding=True`). Verificado en vivo en PRE vía API del widget: fauna responde del KB, precios intactos.
- **⚠️ Lección operativa (2026-07-10)**: las sondas automatizadas contra el Chatwoot de PRE (crear sesiones del widget repetidamente) disparan el rate-limit de Rack::Attack y **bloquean la IP compartida ~20 min** — incluye el navegador de quien esté probando (la burbuja del widget desaparece). Espaciar sondas (≥1/min), no poner bucles de reintento contra `/widget`, o probar con datos móviles (otra IP).
- **2026-07-10 — merge de Gonzalo (27 commits, v0.20.3-v0.20.7 + Coral)**: fast-forward limpio a `feature/dev_alvaro`, suite verde tras merge, todo desplegado a PRE junto con lo anterior.
- **2026-07-09 (feature/pruebaGon) — 3 bugs reportados por el owner arreglados + batería exhaustiva de "respuestas a preguntas del bot"**: **(BUG2)** nombrar/tener una certificación PADI ("soy open water", "tengo el advanced", "soy rescue diver", "soy divemaster", "i am open water", "tengo nitrox") ya NO pregunta "¿eres certificado?" — nuevo `_holds_padi_cert()` (`intent_detector.py`) marca `is_certified=True` y distingue tener el título de querer el curso ("quiero el curso open water" sigue no-certificado); además "soy open water" ya no se clasifica como el curso Open Water sino como buzo certificado. **(BUG1)** una pregunta de precio genérica ("cuánto cuesta", "precios", "how much") daba fallback/deflectaba → nuevo `_canonical_price_overview_answer` (`rag_agent.py`) da un overview de precios de los servicios principales (leídos de SERVICES, ES+EN); si nombra un servicio específico lo deja a RAG. **(BUG3)** batería exhaustiva de respuestas contextuales a preguntas del bot (sí/no en el flujo con muchas variantes — "claro que sí", "por supuesto", "obvio", "sip", "si lo soy"; última inmersión; ubicación; ofertas RAG "sí quiero/sí necesito"; inglés) → el bot ya las entiende bien en contexto (vía `condense_query` + el clasificador de menú). Único fallo hallado y arreglado: en el paso de ubicación una respuesta vaga ("no sé, tú qué recomiendas", "da igual", "el que sea") daba "no te entendí" → ahora recomienda Cartagena (lo más común) y sigue. Tests en `test_intent_robustness.py`, `test_rag_safety.py`, `test_conversations.py`. Suite: 1517 passed, 15 skipped.

- **2026-07-09 (v0.20.7) — bug real "pack de N" sin unidad se olvidaba + bug real de RAG en precio de 2 inmersiones**: (1) "el pack de 5" (sin decir "inmersiones") nunca resolvía un conteo — el detector exigía siempre la palabra de unidad. Nuevo patrón de respaldo resuelve 5/7/9 sin ambigüedad (nunca son un conteo válido de días); 2/3/4 siguen preguntando porque sí son ambiguos. Auditados los 40 casos (1-10 × dígito/palabra × ES/EN) en `test_intent_robustness.py`. (2) Preguntar "¿cuánto cuestan 2 inmersiones?" en RAG devolvía el precio del paquete de 3 (con nocturna) — no había FAQ curada para "2 inmersiones desde Cartagena" (solo la variante "ya en las islas"), así que el RAG recuperaba el chunk del paquete de 3 (que menciona "2 inmersiones diurnas" en su descripción). Añadida la FAQ que faltaba en `faqs.json`. **⚠️ PENDIENTE: verificar en vivo en PRE tras el reindex** — no se pudo probar localmente (sin `OPENAI_API_KEY`/Postgres+pgvector). Suite: 1480 passed, 6 skipped. Ver `docs/HISTORY.md` v0.20.7.
- **2026-07-09 (feature/pruebaGon) — batería 5: memoria/conversación larga, cambio de moneda, inglés profundo**. Memoria OK (datos tempranos sobreviven detours de info; edades reutilizadas). 3 fixes: **(grupo EN)** "2 certified divers" / "N divers/buzos/buceadores" / "N certificados" no contaban como tamaño de grupo → nuevos patrones en `intent_detector.py` (arregla también el skip de última inmersión en inglés, que dependía de tener el grupo). **(cambio de moneda)** al cambiar el resumen a USD ("muéstrame en dólares"), la línea de extras seguía diciendo "Precio en pesos (COP)" → ahora refleja `mixed_display_currency`. **(nacionalidad, retención)** "somos colombianos" / "extranjeros" / "foreigners" ahora se detectan de forma determinista (`_detect_nationality`, negación-primero) y se persisten a `state.is_colombian`, así el checkout NO re-pregunta "¿eres colombiano?" y usa la moneda correcta. Verificado en vivo: EN1 ("2 certified divers... dived last month... foreigners") va directo al resumen en USD sin preguntar cantidad, última inmersión ni nacionalidad. Tests en `test_intent_robustness.py`. Suite: 1406 passed, 15 skipped.
- **2026-07-09 (feature/pruebaGon) — batería de casos raros/nuevos (~40, live) + auto-deploy a PRE para Gonzalo**: probados poca/mucha info, contradicciones, off-topic, adversarial (inyección de prompt, pedir nº de tarjeta), números absurdos, pagos raros (bitcoin/euros/cuotas), médico (asma/embarazo/hipertensión → escalan), logística (perro/wifi/gopro/mareo), typos/slang, quejas, sin-sentido. **Balance: muy robusto.** 1 bug arreglado: **food-hijack** — un info-dump de reserva (grupo + presupuesto + "¿qué recomiendan?") que menciona una palabra de dieta recibía SOLO la respuesta canónica de comida; nuevo `_FOOD_HIJACK_GUARD` (`rag_agent.py`) hace que el canónico de comida ceda a RAG ante señales de reserva amplia (grupo/cert/presupuesto/recomendación/paquete). Tests en `test_rag_safety.py`. **🟡 Menores NO arreglados**: (1) nitrox "$10 por tanque" es probable alucinación (el KB solo tiene la especialidad Nitrox a $382; el guard de moneda no lo atrapa porque "10" está en otros números del contexto, y el path RAG del supervisor usa `verify_grounding=False`); (2) "?" solo resetea a selección de idioma; (3) emoji/"..." → welcome en inglés; (4) "quiero el curso open water" pregunta certificación en vez de ir directo a cursos. **Deploy a PRE**: se añadió `feature/pre_pruebaGon` al trigger de `deploy-pre` en `ci.yml` (rama espejo, mismo patrón que pre_gadea/pre_alvaro) + `scripts/deploy_pre_gon.sh` (deploy en un comando).
- **2026-07-09 (feature/pruebaGon) — batería 3 (retención de info: "nada perdido ni re-preguntado") + SP2**. Enfocada en que todo lo que el cliente dice en un mensaje se capture y NO se vuelva a preguntar. 3 fixes: **(SP2)** menores <10 no pueden estar certificados (edad mín. OW=10) → `_split_out_uncertifiable_kids` (`intent_detector.py`) los saca del grupo certificado ("familia de 5: papá y mamá certificados, 3 niños de 6/9/13" ahora es 3 cert + 2 beginners; un 13-14yo SÍ puede ser cert, así que se queda). **(última inmersión)** "buceamos hace un mes / hace 6 meses / no hace más de 2 años / dived recently" ya NO se re-pregunta — `_detect_last_dive` ampliado (formas en palabras, verbo opcional, "recientemente"/"menos de N años"/"N años sin bucear", ES+EN, gateado a contexto de buceo) + `_skip_last_dive_if_known` (`decision_tree.py`) salta la pregunta y aplica refresher si >2 años o preview si no. **(cantidad)** "para 1 persona" (singular) ahora cuenta como grupo. Verificado en vivo: IR1/IR9 (dumps con última inmersión) van directo al preview con precio; IR2 no re-pregunta cantidad. Tests en `test_intent_robustness.py`. Suite: 1372 passed, 15 skipped.
- **2026-07-09 (feature/pruebaGon) — batería 2 (más profunda): cursos, edades límite, splits, multi-turno, checkout/pago/acompañante end-to-end**. Confirmado en vivo: routing de cursos (open water/advanced entran al flujo, "el hallazgo #4 de C2 solo se disparaba con la negación 'no quiero bucear' previa"), auto-select de 2-buceos (EN), apodo "ciudad amurallada"→Cartagena, y el **checkout completo como colombiano muestra el nuevo resumen (#4 TOTAL único sin conversión) + el mensaje de divisa (#5)**. 1 bug arreglado: **AG4** — la nota de elegibilidad para 12-17 años omitía que *Advanced/Rescue* están disponibles desde los 12 (saltaba directo al límite de Divemaster@18); `age_eligibility_note` (`eligibility.py`) ahora lo confirma (ES+EN), tests en `test_eligibility.py`. **🟡 Hallazgos NO arreglados**: (SP2, medio) "familia de 5: papá y mamá certificados, 3 niños de 6/9/13" se parsea como "5 buzos certificados" (ignora que los 3 niños son no-certificados) — fix robusto arriesga regresiones porque un 13-14yo SÍ puede ser buzo certificado, así que no se puede asumir "menor=no-cert"; (MT1, menor) tras "quiero bucear"→"somos 4" RAG asume islas+certificados; (MT2, menor) recomposición enrevesada cuando snorkel→5→"uno quiere bucear certificado" acaba en preview de minicurso para 5. Suite: 1359 passed (+ AG4).
- **2026-07-09 (v0.20.5) — bug real: el bot se saltaba isla/hotel cuando la ubicación venía del primer mensaje libre**: reportado en vivo en PRE por Gonzalo (captura de Chatwoot, mismo día que v0.20.4). Cuando `location=="island"` se detectaba directo del PRIMER mensaje (isla nombrada, o el fallback genérico de v0.20.4), dos puntos de `supervisor.py` (`_should_skip_to_certified_flow` y el camino de grupo mixto) resolvían el plan sin preguntar isla/hotel — el resumen final asumía silenciosamente "Islas del Rosario" con horario de recogida inventado. El camino equivalente vía la pregunta explícita `MIXED_LOCATION` ya lo hacía bien; corregido replicando la misma comprobación (`_goto_island_hotel_menu_or_unknown`) en los 2 puntos que faltaban. Confirmado en ES+EN. Suite: **1353 passed**, 6 skipped. Ver `docs/HISTORY.md` v0.20.5.
- **2026-07-09 (v0.20.4) — bug real de ubicación reportado en vivo en PRE (captura de Gonzalo) + auditoría de apodos ES/EN**: "vamos desde las islas" (sin nombrar isla/hotel específico) nunca fijaba `location` en `_detect_location` (`intent_detector.py`) — el bot repreguntaba "¿desde dónde sales?" aunque el cliente ya lo hubiera dicho. Arreglado con un patrón de respaldo ES+EN. De paso: (1) auditoría de paridad ES/EN encontró que el conteo de días en palabras ("a two-day package") no funcionaba en inglés — corregido; (2) investigados y añadidos apodos reales de Cartagena/islas vía búsqueda web (ES+EN: "la heroica"/"the heroic city", "corralito de piedra", "ciudad redentora", "la ciudad amurallada"/"the walled city", "queen of the caribbean", "los rosarios"/"the rosarios"); (3) encontrado y arreglado un bug preexistente (no de esta sesión) donde el patrón bare `\brosario\b` colisionaba con "rezar el rosario" (oración católica común en Colombia). Suite: **1347 passed**, 6 skipped. Ver `docs/HISTORY.md` v0.20.4.
- Main collaboration branch: `feature/dev_alvaro` (Gadea trabaja en `feature/dev_gadea` y se mergea periódicamente; Gonzalo en `feature/pruebaGon`).
- **2026-07-08 (v0.20.3, feature/pruebaGon) — 8 ajustes de UX/flujo pedidos por el owner** (todos con tests + verificados en vivo): (#1) "soy certificado" entra al flujo de buceo (override determinista en `supervisor.py` cuando el agente subclasifica un statement de buzo como pregunta; guard "?" mantiene preguntas en RAG); (#2) "¿qué ofrecen para bucear?" → overview curado canónico en `rag_agent.py` (dónde se bucea + minicurso/paquetes/cursos/snorkel), con guard para no secuestrar precio/inclusión; (#3) **quitada la pregunta proactiva de lancha privada** (va directo al resumen; RAG responde si preguntan) — `MIXED_FINAL_PRIVATE` queda huérfano; (#4) resumen con **un único TOTAL** (sin "TOTAL ESTIMADO" ni conversión ≈); (#5) la pregunta "¿colombiano?" aclara que no hay cobro extra por divisa; (#6) no-colombianos **sin "pagar en persona"** (100% online); (#7) colombianos: "100% online" o "50/50" (botones de resumen dinámicos por nacionalidad, ambos escalan con el método anotado); (#8) **upsell de acompañante** (nuevo step `MIXED_COMPANION_UPSELL` al elegir acompañante en el menú: minicurso/snorkel/"solo acompañar"; el acompañante mantiene su precio ~$80/288k, decisión del owner) + **intro amable/positiva en preguntas genéricas** de buceo (prompt RAG ES+EN). Ver `docs/HISTORY.md` v0.20.3. El upsell de acompañante se dispara por el menú (choice 5), por el classifier, Y por **detección proactiva de mención libre** (`_mentions_pure_companion`/`_enter_companion_upsell` en `supervisor.py`): "voy con mi novia que solo acompaña" → pregunta origen y luego ofrece minicurso/snorkel/solo-acompañar. Solo cuando el hablante no tiene intención de buceo propia y no es pregunta.
- **2026-07-08 (v0.20.2) — bug del paso "¿cuántas personas?" (MIXED_ADD_QTY), 2 familias**: (A) conteo natural con acompañante ("yo y mi pareja me acompaña") daba "no te entendí" → `_parse_mixed_quantity` lo reconoce como 2; (B) "2 y uno que quiere hacer snorkel" descartaba al de snorkel → nuevo `_detect_cert_qty_activity_split` parsea ambos subgrupos y enruta al split con conteos correctos. Tests en `test_companion_split.py`. Suite 1279. **🟡 Follow-up NO arreglado**: el mismo tipo de mensaje entrando por el paso hermano `MIXED_ASK_CERTIFICATION` (entrada por texto libre, no por botón del plan) tiene un bug análogo — `_apply_group_recomposition` (supervisor.py) cuenta mal "yo y mi pareja me acompaña" como 3, y "2 y uno snorkel" se mal-rutea a snorkel-only. Reproducir: entrar con "quiero 2 inmersiones 1 dia desde cartagena" y responder con esas frases. Requeriría llevar la misma lógica de `_detect_cert_qty_activity_split`/companion-count al camino de `MIXED_ASK_CERTIFICATION`/recomposición.
- **2026-07-08 (v0.20.0/.1) — reconocimiento de audio + herramienta `/audio-test` + rediseño UI**: notas de voz se transcriben (OpenAI) en el borde de la ingesta (`src/channels/audio.py`, helper `_resolve_voice_note` usado por webhook Y poller); página `/audio-test` con grabador de micro para probar sin WhatsApp; `/chat` y `/audio-test` rediseñadas con UI de marca "bajo el agua". Ver `docs/audio-voice-transcription-plan.md`.
- **2026-07-08 (v0.20.1) — herramienta de prueba de audio `/audio-test`**: como el widget web no tiene micrófono, se añadió una página que graba con el navegador y muestra transcripción + respuesta del bot (deshabilitada en producción). **SÍ desplegada a PRE** (`feature/pre_alvaro`) para que el owner/equipo pueda probar la transcripción en `https://pre.is-core.dev/audio-test`. Refactor `transcribe_audio_bytes` reutilizable. Suite: 1250 passed.
- **2026-07-08 (v0.20.0) — reconocimiento de audio, Fase 1 (MVP)**: las notas de voz del cliente ahora se transcriben (OpenAI `gpt-4o-mini-transcribe`) en el borde de la ingesta y entran al pipeline como texto; nada aguas abajo cambia. Módulo `src/channels/audio.py` + helper compartido `_resolve_voice_note` en `chatwoot.py` (usado por webhook Y poller — ambas rutas). Fallback amable si la transcripción falla. **NO desplegado a PRE** (el widget web no graba voz; latente hasta WhatsApp). Plan completo y fases 2-3 en `docs/audio-voice-transcription-plan.md`. Suite: 1248 passed, 15 skipped. **Para el equipo**: si continúan, la Fase 2 (eco de confirmación del transcript / nota privada para el asesor) es lo siguiente recomendado — importante por la sensibilidad a transcripciones erróneas ("somos 2"→"somos 12").
- **2026-07-08 (v0.19.31) — cierre de los 2 gaps documentados al final de v0.19.30**: (1) cambio de ubicación a mitad de flujo (antes de confirmar al carrito) ahora remapea también el plan PENDIENTE (`state.mixed_pending_qty_plan`), no solo los items ya confirmados — `_remap_cart_for_location` extendida + `orchestrator_set_location` ya no se salta el remap cuando el carrito está vacío. (2) el detector de splits de grupo mixto (`_detect_group_info`) ya no falla si el cliente inserta un conteo de inmersiones/días entre la actividad y el "y" ("3 buceamos certificados 5 inmersiones y 2 hacen snorkel") — antes perdía silenciosamente a los de snorkel, tratando a todo el grupo como certificado. Tests de regresión en `test_cert_multiday_matrix.py`/`test_intent_robustness.py`. Suite: 1234 passed, 15 skipped. Ver `docs/HISTORY.md` v0.19.31.
- **2026-07-08 (v0.19.30) — bug de enrutamiento del cert multi-día (reportado por Gonzalo) + soporte de conteo por días**: "el paquete de 2 buceos" se enrutaba mal al botón "Paquete multi-día" (interceptado por `_match_quick_reply_text` antes del corto-circuito de conteo de inmersiones de Gonzalo) — corregido reordenando los checks en `supervisor.py`. Al profundizar se arreglaron 2 gaps más (conteo perdido si se menciona antes de conocer la ubicación; "4 inmersiones" en isla es ambiguo → nuevo submenú) y se añadió detección de conteo por DÍAS ("un paquete de 3 días") con sus propios submenús para los casos ambiguos (1 y 2 días). Se encontró y arregló una regresión propia durante el desarrollo de tests: `_reset_mixed_state` no limpiaba los campos nuevos, dejando un submenú "pegado" tras un reinicio. **2 gaps documentados en código pero NO arreglados** (ver "Next session priorities" abajo): remap de plan pendiente al cambiar ubicación a mitad de flujo, y fragilidad del detector de splits de grupo mixto ante conteos insertados dentro de la frase de split. Nueva suite dedicada `tests/test_cert_multiday_matrix.py` (102 tests, matriz completa de casos). Suite: **1231 passed**, 6 skipped. Ver `docs/HISTORY.md` v0.19.30 para el detalle completo.
- **Despliegue a PRE**: desde v0.19.25, el job `deploy-pre` se dispara con push a `feature/pre_gadea` **o** `feature/pre_alvaro`, y siempre despliega la rama real que hizo el push (parametrizado con `github.ref_name`, ya no hardcodea `feature/pre_gadea`). `feature/pre_alvaro` es una rama espejo de `feature/dev_alvaro` (mismo patrón que ya usaba Gadea): el trabajo normal sigue en `dev_alvaro`, y solo cuando quieras desplegar de verdad a PRE actualizas `pre_alvaro` (`git push origin dev_alvaro:pre_alvaro --force-with-lease`) — así un commit rutinario no dispara un rebuild+reindex en PRE. PRE sigue siendo UN solo entorno compartido (un único `dp-pre-bot`/`dp-pre-postgres`) — decisión explícita del equipo de no montar una segunda infraestructura; quien despliega último "gana" el estado de PRE. Ver `docs/deploy-pre-redeploy.md`.
- **2026-07-07 (v0.19.27) — batería de tests: reconfirmación formal completa, las 176 casillas tachadas**: probadas en vivo una a una las categorías 4, 6, 8-22 (las únicas que quedaban sin checkbox individual) contra `route_message()` real con LLM + pgvector local reindexado, usando el nuevo `scripts/live_battery_driver.py` (driver reutilizable para conversaciones multi-turno). **La batería queda 176/176 con checkbox.** 5 fixes reales: (1) Divemaster nunca se mencionaba en `age_eligibility_note()` — ahora lo hace para edades 12-17; (2) falso positivo médico con "presión" coloquial ("buena presión para manejar grupos") — nueva exclusión de modismos en `escalation.py`; (3) descuento de equipo propio confirmado con equipo PARCIAL (determinista, 3/3) — regla explícita nueva en el prompt; (4) inmersión extra confirmada sin mencionar que casi nunca hay tiempo operativo — FAQ nueva + reindex; (5) instalado `tzdata` en el entorno de desarrollo Windows (faltaba desde el fix de fecha/hora de v0.19.25, solo afecta a dev local). **2 riesgos de flakiness ya conocidos, reconfirmados pero NO resueltos** (no son regresiones): el verificador de grounding sigue rechazando intermitentemente (~1/3) respuestas correctas — más visible en conversaciones largas de memoria; y la regla contra inventar capacidad de tour privado (T114) no se sigue siempre a temperatura 0.3. Suite: 1099 passed, 6 skipped. Reindex hecho en dev; pendiente en PRE/PRO (solo por el FAQ nuevo). Ver `docs/test-battery-edge-cases.md` sección "cont. 11" para el detalle completo.
- **2026-07-07 (v0.19.26) — batería cat. 1/3/5/7 con checkbox (22 tests) + 7 fixes**: barridas en vivo las 4 categorías que quedaban con huecos de primera prueba; 22 checkboxes marcados y 7 fixes reales aplicados, incluido el bug reportado por el owner en PRE ("si" tras la oferta de "¿te paso con un asesor?" caía al fallback genérico — nueva rama determinista de afirmación-tras-oferta en `route_message`). También: cupo inventado para "mañana" (regla de prompt), edades sin respuesta (inyección de `age_eligibility_note()` en extra_context), detector de edades ("anos" sin ñ, "mi hijo, tiene 7"), confirmación de grupo mixto que borraba a los menores, e idioma reforzado ante términos EN. Suite: 1091 passed. **Para el equipo: quedan las categorías 4, 6, 8-22 por reconfirmar formalmente (tachar checkboxes)** — ya barridas en prosa en sesiones anteriores; ver registro "cont. 10" de `docs/test-battery-edge-cases.md` con el orden recomendado. Los cambios se despliegan a PRE actualizando `feature/pre_alvaro` (`git push origin dev_alvaro:pre_alvaro`).
- **2026-07-07 (v0.19.25) — 3 arreglos puntuales tras pruebas en vivo en PRE**: (1) alucinación de "el corte de reserva ya pasó" ante "quiero reservar para mañana" — el RAG no tenía ninguna noción de la hora/fecha actual; arreglado inyectando la fecha/hora real (`America/Bogota`, `zoneinfo`) en `_build_extra_context` + nueva regla de prompt (ES/EN) que prohíbe afirmar que un corte de horario "ya pasó" sin poder compararlo contra la hora real. (2) botón "🔄 Nueva conversación" en `/chat` (`src/main.py`) para que el owner pueda resetear el widget de Chatwoot con un clic en vez de usar incógnito. (3) PRE ahora se despliega también desde `feature/pre_alvaro` (ver bullet de arriba). Suite: 1081 passed, 6 skipped.
- **2026-07-07 (v0.19.24) — cierre de los 5 gaps menores restantes (T007, T011, T013, T123, T165)**: T007 requirió un fix arquitectónico real (no un parche): la tool `remember` del orquestador solo soportaba un split binario cert/no-cert — añadido `snorkel_count` al schema + `_persist_remembered`/`_route_detected_intent` (`supervisor.py`) ahora soportan `group_allocation` de 3 vías, así que "minicurso hija + snorkel yo + paquete esposo" en una frase ya no pierde silenciosamente a la persona de snorkel. T011: el hotel mencionado junto a un cambio de origen ahora se persiste (el orquestador solo ejecuta una tool primaria por turno; reforzada la descripción de `remember`'s hotel + fallback defensivo por una clave `island` que el modelo a veces inventa). T013: nuevo detector determinista para nacionalidad mixta del grupo, responde honesto en vez de fallback genérico (split real de facturación queda fuera de alcance). T123: nudge hacia servicio privado/asesor para grupos >=15 sin inventar capacidad máxima. T165: nuevo patrón de suma para sustantivos con género ("N buceadoras y M buceadores"). Suite: 1078 passed, 6 skipped, sin cambios de KB. Ver `docs/test-battery-edge-cases.md` sección "cont. 8".
- **2026-07-07 (v0.19.23) — Categoría 2 (cambio de opinión, T008-T015) barrida en vivo**: no tenía registro de sesión confirmado. 2 bugs reales encontrados y arreglados: (1) `_GROUP_RECOMPOSE_RE`/`_apply_group_recomposition` (`supervisor.py`) no cubría "en realidad somos 5" (sin ahora/ya) ni "se me olvidó mi cuñado" — ampliado el regex; (2) el LLM inventaba confirmaciones de cambio de reserva ("¡Listo! Cambiamos la reserva al 20") en ~4/6 intentos ante "mejor cambiémoslo al 20" sin que existiera una reserva real — reforzada la regla del prompt (ES+EN) contra confirmaciones inventadas. Resto de la categoría (T009-T015) confirmado correcto o son gaps ya conocidos que degradan con gracia (T011: hotel mencionado junto al cambio de origen no se persiste dentro del flujo de carrito; T013: nacionalidad mixta del grupo, no implementada). Suite: 1078 passed, 6 skipped, sin cambios de KB. Ver detalle en `docs/test-battery-edge-cases.md` sección "cont. 7".
- **2026-07-07 (v0.19.22) — bug del acompañante inventado (T113), CERRADO**: se mergeó `feature/pruebaGon` (con todo el trabajo v0.18.0→v0.19.21) en `feature/pre_gadea`, y se retomó el bug sin resolver de T113. Causa raíz real encontrada (distinta de lo investigado en la sesión que lo descubrió): `scripts/load_embeddings.py` indexaba las conversaciones reales de WhatsApp (`conversations.json`) incluyendo las citas LITERALES del cliente ("Cliente dice:\n- ... mi esposo...") como contenido buscable del KB — separado del bloque few-shot (ya sanitizado desde v0.19.14). `source_weight_for_topics()` (`vector_store.py`) boostea esa fuente `conversations` (+0.10 a +0.25) justo para topics como `location_islands`/`meeting_point`/`payment`, que son los de "estoy en la isla, hotel X". Confirmado con consultas directas a la DB: una transcripción de otro cliente mencionando "mi esposo" puntuaba 0.496 (justo bajo el umbral individual 0.50) pero el boost de rerank la metía en el contexto igual, junto a las FAQs confiables — el LLM a veces mezclaba ese dato ajeno. Fix: ya no se indexan las citas literales del cliente (solo escenario + respuesta del asesor + temas) + etiqueta explícita "otro cliente distinto" en el contexto de `rag_agent.py`. Verificado 18/18 sin alucinación (antes ~3-4/5). **Reindexado en dev, pendiente en PRE/PRO.** Suite: 1078 passed, 6 skipped. Ver detalle completo en `docs/test-battery-edge-cases.md` sección "cont. 6".
- **2026-07-07 — batería de tests conversacionales + bug de acompañante inventado (sin resolver)**: se creó `docs/test-battery-edge-cases.md` (168 casos, 22 categorías, con checkboxes + registro de sesión) para ir cazando fallos de calidad conversacional de forma sistemática. Se empezó a probar Categoría 16 (sobre-personalización): T110/T111/T112 pasaron en vivo (sin marcar checkbox aún, ver nota de re-confirmación en el propio doc). **T113 destapó un bug real y sin resolver**: el bot puede inventar un acompañante que nunca se mencionó (ej. "estoy en el hotel Cocoliso" → "tu esposo puede..."). Se aplicaron 2 mejoras en `rag_agent.py` (quitar cita literal del cliente en los ejemplos few-shot; regla explícita "nunca inventes acompañantes" en el prompt ES+EN) pero la alucinación persiste (~3-4/5 en pruebas repetidas) y la causa raíz exacta no se aisló — la reconstrucción del pipeline en un script directo no reprodujo el fallo (0/12), así que algo del flujo en vivo real difiere de la réplica. **Necesita logging en el proceso del bot en vivo para cerrar** (ver detalle completo de la investigación en la entrada de sesión dentro de `docs/test-battery-edge-cases.md`). Suite: 1022 passed, 6 skipped, sin regresiones.
- **2026-07-06 (v0.18.0 → v0.19.13 — sesión larga en `feature/pruebaGon`: negocio + tono + elegibilidad/auto-armado + E2E real)**: sesión extensa de Gonzalo. `feature/pruebaGon` ahora contiene TODO el trabajo de `dev_gadea`/`pre_gadea` mergeado (agente Fase 1, Redis, PRE, PADI free-text) MÁS lo de esta sesión. Resumen por bloques (detalle en `docs/HISTORY.md`):
  - **Descuento colombiano ELIMINADO** (decisión del owner, v0.18.0): ya no existe; internacionales pagan USD, colombianos/residentes COP (mismo precio). La pregunta "¿eres colombiano?" se conserva SOLO para elegir moneda. Limpiado en KB (`discounts.json`/`policies.json`/`faqs.json`/`conversations.json`) + código (`decision_tree`/`lead_summary`/`supervisor`/`rag_agent`). También un filtro few-shot (`_example_teaches_stale_colombian_discount` en `rag_agent.py`) que descarta ejemplos históricos donde el asesor ofrecía el descuento viejo.
  - **Tono costeño de Cartagena**: tuteo cálido ("tú", NO "usted") + expresiones colombianas. `brand_tone.json` + prompt RAG hardcoded (`_SYSTEM_PROMPT_ES_BODY`; ojo: `_build_tone_section` solo lee personality+human_touches[0]+age, por eso las reglas fuertes van hardcoded en el prompt).
  - **Framing SIEMPRE positivo** (prompt RAG ES+EN): nunca decir que la elección del cliente (fecha/mes/estación/lugar/nº personas/comparación) es mala; sacar lo positivo, sin inventar ni ocultar seguridad. Verificado en vivo (incl. "¿mejor aquí o San Andrés?" sin desacreditar).
  - **Motor de elegibilidad por edad** (`src/flows/eligibility.py`, NUEVO — fuente única de verdad): snorkel 6+, Bubble Makers 8-10, minicurso/OW 10+, Advanced 12, Divemaster 18, buceo certificado=OW+10. `activities_for_age`, `can_fun_dive`, `age_eligibility_note`, `plan_group`/`format_group_plan`. Detección de edades en `intent_detector._detect_ages` (+ `DetectedIntent.ages`), memoria multi-turno (`state.detected_ages`), y respondedor determinista `supervisor._maybe_answer_age_eligibility`.
  - **Auto-armado por edad** (`decision_tree.py`): split de acompañante no-buzo en el paso de cantidad (`_reveals_non_certified_companion`/`_start_cert_companion_split`), oferta adaptada por edad (menor <10 no recibe minicurso adulto), y COLA interactiva multi-persona (`mixed_pending_beginner_queue` + `_process_beginner_queue`) que coloca a cada no-certificado según su edad, uno a uno.
  - **Escalación de quejas/fraude** (`escalation.py`): "estafa/scam/pésimo servicio/reembolso exigido" ahora escalan (antes iban a RAG).
  - **Robustez del detector** (`intent_detector.py`): "no soy certificado"→False; "solo snorkel"/"no quiero bucear"→snorkel; edades en inglés "ages 8 and 10"; bare "buzos"/"somos buzos"→certificado (con guards "no somos buzos"/"quiero ser buzo"); split verbal "3 buceamos certificados y 2 hacen snorkel" (plural `certificad[ao]s?`).
  - **⚠️ FIX OPERATIVO CRÍTICO — `RAG_MIN_SCORE` 0.72 → 0.50**: el umbral de coseno era demasiado alto para `text-embedding-3-small` (coincidencias reales ~0.60-0.67) → causaba el "fallback intermitente" que arrastrábamos (buceo adaptado, precios COP caían al "no tengo información" pese a estar en la KB). Corregido en `.env.example`/`.env.dev.example` (tracked) y en el `.env` local de dev. **PRE y PRO todavía tienen 0.72 en sus `.env` del VPS — hay que cambiarlo a 0.50 al redesplegar** (ver `docs/deploy-pre-redeploy.md`). Las guardas de grounding (montos/URLs) siguen protegiendo contra alucinaciones.
  - **E2E real validado**: se levantó Docker + Postgres/Redis local, se reindexó la KB (779 docs), y se corrieron ~25 conversaciones con el LLM real (gpt-4o + pgvector) — framing, sin-descuento, routing, currency COP/USD, flujos completos a checkout, escalaciones, buceo adaptado, multi-turno: todo OK. Gotcha para correr el bot desde el host Windows: la `DATABASE_URL` del `.env` usa `host.docker.internal` (no resuelve desde el host Windows) → override a `localhost` (`export DATABASE_URL=postgresql://postgres:postgres@localhost:5432/diving_planet`, scheme plano SIN `+asyncpg` para `load_embeddings`).
  - **Runbook de redeploy PRE**: `docs/deploy-pre-redeploy.md` + `scripts/redeploy_pre.sh` (one-shot: pull + RAG_MIN_SCORE=0.50 + rebuild `dp-pre-bot` + reindex + verify). Sin migraciones de DB nuevas.
  - **Follow-ups pendientes** (no críticos): (1) un split de acompañante descrito en el paso de UBICACIÓN (no de cantidad) no se auto-detecta — degrada con gracia; (2) el bot responde "capital de Francia" desde conocimiento del LLM antes de redirigir (inofensivo); (3) precios RAG con decimal crudo ("$125.57"); (4) el cableado interactivo del auto-armado solo se probó determinista + parcialmente en E2E.
  - Suite: **1022 passed**, 15 skipped, 0 fallos. Docker/Postgres/Redis local quedaron levantados al cierre.
- **2026-07-04 (v0.19.2 — verificación en vivo del agente conversacional "Fase 1" + 4 fixes)**: rama `feature/pre_gadea` creada desde `origin/feature/dev_alvaro` (tip `59a90ae`, el commit "understanding-first conversation agent"). El owner mandó 8 conversaciones de prueba (capturas) que motivaron ese refactor; en vez de inferir del texto de las capturas, se levantó Postgres+Redis local (`docker compose up -d postgres redis`, KB ya cargado con 779 docs) y se corrieron las 8 conversaciones reales contra `route_message()` con un script ad-hoc. 6/8 ya funcionaban; se encontraron y arreglaron 2 problemas reales + 2 más al testear manualmente el flujo de "Reservar" después:
  - `ESCALATION_KEYWORDS` en `supervisor.py` tenía las palabras sueltas `"persona"/"person"` (para detectar "hablar con una persona"), que hacían falso-positivo con cualquier frase que dijera "una persona" contando gente (ej. "hay una persona que tiene 14 años") — escalaba sin responder nada, saltándose el agente conversacional nuevo. Quitadas; `"hablar con"/"talk to"/"speak with"` ya cubren la petición real de humano.
  - La preferencia explícita de "no separar al grupo" se capturaba vía `remember` (`state.remembered_facts["preference"]`) pero nunca se usaba — el carrito mixto partía al grupo (cert + minicurso) sin ningún acuse. `_build_confirmation_message` ahora añade una frase de tranquilidad si detecta esa preferencia.
  - Al probar el flujo de "🤿 Reservar" manualmente (con un servidor local en el puerto 8000, ver más abajo), se vio que el clic en Reservar reiniciaba el discurso del carrito ("vamos a armar tu reserva paso a paso") aunque el agente ya supiera actividad/personas — y que, tras responder la ubicación, igual repreguntaba "¿qué actividad quieres añadir?" aunque el grupo fuera homogéneo. Ambos arreglados: `_enter_booking_cart` salta la intro genérica si hay `detected_activity`/`detected_group_size`/`remembered_facts` previos (delega en `_handle_mixed_entry`); `_after_location_set` salta el menú de actividad si `state.detected_activity` ya resuelve un único tipo (minicurso/snorkel/certificado) y el carrito sigue vacío.
  - Causa raíz de por qué un grupo claramente principiante ("nunca hemos hecho buceo antes") se detectaba como "buceo certificado": el regex de `intent_detector.py` para "nunca he buceado" solo cubría esa conjugación exacta, no "nunca **hemos hecho** buceo" (auxiliar distinto + "hecho buceo" en vez de "buceado"). Ampliado en los dos sitios donde se usa (actividad y certificación).
  - **Gotcha de testing local**: un servidor `python -m src.main` corriendo en el puerto 8000 (sin `--reload`) sirve código stale si se editan archivos después de arrancarlo — hay que reiniciarlo para ver los fixes reflejados en el widget de Chatwoot local. Si algo "no funciona" al probar manualmente pero sí funciona en un script/test directo, sospechar primero de esto antes de asumir que el fix está mal.
  - RAG baseline (`scripts/eval_rag_answers.py`) re-corrido tras el `verify_grounding=False` de `59a90ae`: sigue **39/39**, confirmando que ese punto pendiente del commit anterior no es un riesgo.
  - **Pendiente/riesgo no resuelto**: el mismo mensaje a veces cae en el fallback "No tengo información suficiente..." en corridas distintas (rechazo no determinista del verificador de grounding con montos) — no investigado a fondo, revisar si se repite en vivo.
  - Suite: **883 passed**, 6 skipped (sin regresiones). Ver `docs/HISTORY.md` v0.19.2 para el detalle completo.
- **2026-07-03 (v0.19.0 — Redis + PRE desplegado y probado en vivo)**: los dos bloqueantes técnicos restantes antes de PRE quedaron resueltos en la misma sesión:
  - **Estado conversacional → Redis** (`src/state_store.py`): reemplaza los 3 dicts en memoria de `chatwoot.py` (`conversations`/`processed_chatwoot_messages`/`conversation_poll_started_at`). TTL deslizante 30 días, lock por conversación (`asyncio.Lock`) para la carrera webhook/poller, set de conversaciones activas auto-limpiante. `conversation_pending_echo_titles` se queda en memoria a propósito (bajo riesgo). Verificado con reinicio real de proceso — el estado sobrevive. Tests: `tests/test_state_store.py` (8, sin Redis) + `tests/test_state_store_integration.py` (9, Redis real — nuevo servicio `redis` en `.github/workflows/ci.yml`).
  - **PRE desplegado de verdad**: VPS Hetzner CX23 (`docker-compose.vps.yml`: Caddy + Postgres/Redis PRE + bot + Chatwoot con DB/Redis propios, ya no comparte `dp-pro-postgres`/`dp-pro-redis`). Dominio temporal `is-core.dev` (HTTPS real vía Let's Encrypt) mientras se gestiona el acceso a HostGator de `divingplanet.org` — ver sección "Environment details" para la URL final y por qué es temporal. Probado end-to-end en navegador real: el bot responde en vivo.
  - **Fix Dockerfile**: no copiaba `alembic.ini`/`alembic/` a la imagen — bloqueaba `alembic upgrade head` dentro del contenedor.
  - **Fix Caddy (gotcha para el futuro)**: Caddy descarta cabeceras HTTP con guion bajo (`api_access_token`) antes de reenviarlas — rompía la auth del bot contra la API de Chatwoot a través del reverse proxy público, aunque el mismo token funcionaba perfecto por la red interna de Docker. Solución: nuevo `settings.chatwoot_api_url` (env `CHATWOOT_API_BASE_URL`) — las llamadas del propio bot a Chatwoot (mandar mensajes, poll, asignaciones) van por el hostname interno de Docker (`http://dp-chatwoot:3000`); `chatwoot_base_url` se sigue usando tal cual para lo que ve el navegador (CORS + widget SDK en `/chat`). Si en el futuro se necesita otra cabecera con guion bajo pasando por Caddy, aplica el mismo patrón (bypasear el proxy, no pelear con `header_up`).
  - Eliminados `docker-compose.public.yml`/`docs/PUBLIC_TESTING.md` (túnel Cloudflare de pruebas rápidas), sustituidos por el despliegue real.
- **2026-07-03 (v0.19.1 — trabajo pendiente de commitear de sesiones anteriores, revisado y cerrado)**: al hacer `/close-work` de la v0.19.0 quedaron varios archivos modificados sin commitear desde antes de esa sesión (checklist #9/#14/#15/#18/#19, todos ya marcados "Hecho" en `pre-launch-checklist.csv`). Se revisaron uno a uno, se encontró y arregló una regresión real, y se subieron:
  - **PADI sub-curso desde texto libre (#18)**: `intent_detector.py` emite la actividad específica (`padi_open_water`/`padi_advanced`/`padi_rescue`/`padi_divemaster`/`padi_specialty`); `supervisor.py` setea `mixed_pending_qty_plan`/`selected_service` con el curso exacto (antes un `TODO` sin implementar). `tests/test_padi_freetext.py` (22 tests) ya cubría esto pero **no** está en la lista de `pytest` de `ci.yml` — corrida manualmente para validar, funciona, pero si se vuelve a tocar esta área conviene añadirla a CI.
  - **Regresión encontrada al validar (no estaba en el checklist)**: correr la suite COMPLETA (`test_conversations.py`, fuera de la lista de CI) reveló que el fix de arriba desviaba preguntas informativas que mencionan un curso por nombre ("¿cómo se paga el curso de divemaster?") hacia el carrito de reserva en vez de a RAG. Arreglado con `_message_looks_like_question()` en `supervisor.py` — guarda por presencia literal de `"?"`. Ojo: la primera versión usaba un set de palabras interrogativas (cuánto/cómo/qué…) y rompió 4 tests distintos porque "que"/"como"/"cual" son conjunciones normales en español, no solo interrogativos. **Moraleja para la próxima vez**: `tests/test_conversations.py` no está en `ci.yml` — correr `pytest tests/` completo (no solo la lista de CI) antes de dar por cerrado un fix de routing/intención.
  - **Decisión de grupo mixto (#19)**: confirmado Opción A (pregunta origen antes de montar carrito). `xfail` quitado de `tests/FreeText/test_mixed_group.py`.
  - **Alembic y tests de precios ya no colgados fuera de git (#9/#14/#15)**: `alembic.ini`/`alembic/` y `tests/test_pricing_consistency.py` se usaban en dev y en el propio despliegue de PRE de la sesión anterior, pero nunca se habían subido — ahora sí están versionados.
  - Fix suelto: `Message.metadata` en `src/db/models.py` colisionaba con el atributo reservado de SQLAlchemy `DeclarativeBase.metadata` — renombrado a `msg_metadata` (columna real sigue siendo `metadata`).
  - No se tocaron `.claude/settings.local.json`/`.claude/settings.json` (permisos locales de otro desarrollador, rutas de su máquina — no pertenecen a este repo funcionalmente aunque estén versionados).
  - Suite: **889 passed**, 1 skipped.
- **2026-07-02 (v0.18.3 — primera pasada E2E en Chatwoot real + fixes)**: se mergeó `feature/dev_gadea` (fast-forward hasta v0.18.2) y se hizo la **primera pasada de los 20 escenarios que estaban "cubiertos por tests pero nunca probados en Chatwoot real"**. Todos pasan (conducidos por la API pública del widget vía un driver en scratchpad: `Conversation.say()` sobre `/api/v1/widget/...`, mismo pipeline webhook→supervisor→respuesta). La pasada destapó y **corrigió** 2 bugs + 3 nits (commit `bc6044d`, +25 tests, suite **709 passed**):
  - **Bug real (número tecleado en pasos de cantidad):** un número escrito a mano (no clicado) se descartaba como falso eco de botón, porque el título del botón de cantidad ES el número literal. El antieco (`conversation_pending_echo_titles` en `chatwoot.py`) registraba esos títulos. Fix: no registrar títulos que igualan su valor o son números (`send_chatwoot_message`) + helper `_is_plausible_typed_reply` compartido por webhook y poller (el **poller no tenía** el guard de sí/no que sí tenía el webhook — bug derivado). Clicks reales nunca afectados (llevan `submitted_values` y van por la rama interactiva antes del antieco).
  - **Infra (RAG a media máquina):** faltaba la columna `content_tsv` en la DB local → BM25 caído, RAG solo-vector. Aplicada `migrations/001_add_fts_to_kb_documents.sql`. **Gotcha:** el reindex (`load_embeddings.py`) hace `DELETE FROM` pero NO recrea la columna generada; un setup de DB nuevo debe correr la migración una vez.
  - **Nit 1 (idioma):** `_detect_language` (`intent_detector.py`) usaba substring → "ahi" matcheaba "hi" → inglés. Ahora whole-word.
  - **Nit 2 (euros):** "pagar"/"pago" suelto en `escalation.py` escalaba "¿puedo pagar en euros?". Restringido a frases de problema de pago.
  - **Nit 3 (Divemaster):** respuestas RAG que ofrecen asesor mostraban botones de menú; ahora `_answer_offers_advisor` (robusto a la redacción del LLM) pone botones asesor/inicio.
  - Auditoría actualizada: `readiness-audit-2026-07-02.html` (20/20 live + 5 fixes) y checklist colaborativo `pre-launch-checklist.csv` (raíz del repo, para importar en Google Sheets — el equipo marca estados con formato condicional/tachado).
  - **Matriz hotel→recogida (nuevo dato del owner, `Dudas_V2.docx`):** resuelve la pregunta #19. El owner clasifica ~40 hoteles en 5 categorías (base/muelle propio/camina al centro/camina al muelle/isla privada). Gap vs. el bot: reconoce 25 hoteles, faltan ~21, y detecta 8 que el owner ya no lista (**Pao Pao incluido** — nuestro caso "especial"). **EN INVESTIGACIÓN por el equipo** antes de tocar nada. Propuesta registrada en `TODO.md`: montar `data/knowledge_base/hoteles.json` como fuente única mantenible que reemplace las 2 FAQs sueltas + los regex desincronizados. NO empezar hasta que el equipo confirme los 8 obsoletos.
- **2026-07-02 (v0.18.2 — KB update Q&A owner ronda 4)**: 10 cambios KB implementados desde Q&A del owner. `discounts.json` corregido (group_discount no aplica a PADI courses, no es automático). `faqs.json` +10 FAQs nuevas/actualizadas: Bubble Makers precio ($187 USD), clima/curso pierde día (retoma al día siguiente, cliente paga hotel), llegada tarde/lancha partida (no reembolso), idiomas instructores (ES/EN), eCard (certificación digital), combinar cursos en mismo viaje, máscaras graduadas (2/3/4 dioptrías), equipo para niños (BCD pequeño + botellas), Nitrox bajo pedido ($10/tanque). FAQ clima actualizada: siempre preferir reprogramar antes de reembolsar. KB reindexada. Baseline RAG: **39/39** estable. KB: **779 docs**.
- **2026-07-02 (v0.18.1 — RAG quality + decision-tree bugs)**: 6 fixes cerrados sobre la rama `feature/dev_gadea`. Baseline RAG: **39/39** estable. KB: **761 docs**. Suite: **122 passed**.
  - `es_price_local_cop` ✅: `price_note_es` en `services.json` + `load_embeddings.py` elige `price_note_{lang}` — BM25 ahora indexa "pesos colombianos" solo en chunks ES; chunks EN quedan limpios (evita falsos HALLUCINATED del verifier).
  - `es_hotel_pickup_pao_pao` ✅: FAQ 11083 reescrita — separa San Pedro/Cocoliso (acceso marítimo confirmado) de Pao Pao (coordinamos pero hay que confirmar logística).
  - `colombiano preguntado dos veces` ✅: guardia `if state.is_colombian is not None` añadida en `_handle_location` y en la rama `state.location is not None` de `_goto_location_with_costs` → salta directamente a SUMMARY.
  - `euros routing` ✅: FAQ bilingüe (id 11084) — no manejamos euros; clientes europeos pagan USD y su banco convierte.
  - `MESSAGE_SPLIT` ✅: implementación ya correcta; 2 tests añadidos para fijar el comportamiento (sentinel → 2 mensajes, quick_replies solo al último).
  - `PADI courses location` ✅: `_handle_courses_open_water_origin` lee `state.selected_service`; handlers Advanced/Specialties/Referral comprueban `state.location is None and base_service in ISLAND_SERVICE_MAP` antes de preguntar ubicación.
- **2026-07-01 (RAG quality — sesión 2)**: KB 755 docs (añadida FAQ comprensiva de precios COP). Fix de `query_rewriter._should_condense`: umbral bajado de `>=2` a `>=1` user messages — los follow-ups en conversaciones de 2 turnos ahora se condensan correctamente. Baseline RAG: **39/39** mantenido. `es_price_local_cop` sigue siendo una limitación de contenido (lista 6 de 8 paquetes; 2_dives y minicurso quedan en rank 9-10, fuera del top-8). `top_k=10` se probó y revirtió (inestabiliza el verifier LLM). Ver `docs/rag-prioridades-baseline.md` para detalle.
- **2026-07-01 (post-merge + RAG fix)**: merge de `feature/pruebaGon` en `feature/dev_gadea` (fast-forward). KB reindexada (753 docs). Fix de retrieval EN en `scripts/load_embeddings.py`: los chunks de pricing llevan ahora `[service_id]` como prefijo para mejorar BM25 discriminación entre paquetes similares; `2_dives_1_day:pricing` pasa de posición 4ª a 2ª para queries EN de precio de 2 buceos. Baseline RAG 2026-07-01: **39/39** (antes 38/39, fallo `en_price_2_dives` corregido). Pendientes cualitativos (no afectan el modo pero sí la calidad): `en_followup_islands` (abre catálogo completo en vez de mantenerse en OW), `es_price_local_cop` (no incluye plan de 2 buceos en la lista). Ver `docs/rag-prioridades-baseline.md` para detalle.
- **2026-07-01 (v0.18.0)**: cambios de negocio + tono + calidad, a petición directa del owner:
  - **Descuento colombiano ELIMINADO**: ya NO existe descuento especial por ser colombiano. Modelo nuevo y simple: internacionales pagan en USD, colombianos/residentes pagan en COP (mismo precio, solo cambia la moneda). El precio COP viene fijado en el KB (`price_cop` en `services.json`/`pricing.json`) — el owner lo actualiza al tipo de cambio vigente; NO se calcula con un conversor FX (inventar precios está prohibido). Limpiado en: `discounts.json`, `policies.json`, `faqs.json`, `decision_tree.py` (pregunta COLOMBIAN sigue existiendo pero SOLO para elegir moneda, no descuento; menú de descuentos, resúmenes ES/EN servicio único + carrito mixto, botón del menú), `lead_summary.py`, `supervisor.py` (`_build_extra_context`), prompt `rag_agent.py`. La pregunta "¿eres colombiano/a?" (`Step.COLOMBIAN`/`MIXED_FINAL_COLOMBIAN`) se conserva porque sigue determinando la moneda a mostrar (`mixed_display_currency`).
  - **Tono costeño de Cartagena**: tuteo cálido ("tú", NO "usted" — decisión explícita del owner: Cartagena es costeño, tutea). Actualizado `brand_tone.json` (nueva sección `colombian_language_guidelines`; ojo: `_build_tone_section` en `rag_agent.py` solo lee `personality`+`whatsapp_style.human_touches[0]`+`age_demographic_consideration`, NO la sección nueva — por eso las instrucciones fuertes de tuteo se pusieron TAMBIÉN hardcoded en `_SYSTEM_PROMPT_ES_BODY`). Mensajes del árbol ya usaban "tú"; se añadió calidez a welcome/main_menu/not_understood.
  - **Cancelación/reprogramación**: `CANCEL_BOOKING_PHRASES`/`RESCHEDULE_BOOKING_PHRASES` ampliadas + insensibles a acentos (`_strip_accents` dentro de `_detect_cancellation_request`/`_detect_reschedule_request`; nota: `_strip_accents` se define más abajo en el módulo pero se resuelve en runtime, OK). 15 tests nuevos en `test_conversations.py` (bloque "CANCELACIÓN Y REPROGRAMACIÓN").
  - **Consolidación `discounts.json`**: quitada la entrada duplicada `roverd_web_discount` (mismo 10% online que `direct_booking`, ahora `online_direct_booking`); añadido `own_equipment_discount` (5%, ya se muestra en el menú de descuentos del bot).
  - **Fixes de calidad**: `test_hotel_detection.py` usaba `return` en vez de `assert` (nunca fallaba); código muerto eliminado en `_format_full_itinerary` (`payment_title`, `booking_url`) + import `is_negative`.
  - Suite: 667 passed, 1 skipped, 1 xfailed. **PENDIENTE**: reindexar KB para que RAG sirva los cambios (`$env:ENV_FILE=".env.dev"; python -m scripts.load_embeddings`).
- **2026-06-21 (v0.17.8)**: tres cambios sobre el checkout y la navegación del carrito, a petición directa del owner (agilizar el proceso de reserva para clientes no colombianos):
  - **Link de reserva directo para no colombianos** (`decision_tree.py`): al confirmar el carrito mixto o un servicio único, si el cliente NO es colombiano y hay un `booking_url` resoluble, el bot envía el/los link(s) directamente (10% online) en vez de escalar — `_handle_mixed_final_summary` y el nuevo `_single_service_reservar_response` (sustituye el bloque duplicado que había en `_handle_summary` para `itinerary_offer`/`follow_up`). Colombianos (pago 50/50 + descuento que coordina el asesor) y carritos sin link resoluble (Divemaster/referral) siguen escalando igual que antes. Nuevos helpers `_resolve_service_booking_url`/`_format_booking_links_block`.
  - **Nota de lead sin escalar** (`supervisor.py`): nuevo `state.pending_lead_note_reason` + helper `_maybe_build_pending_note()` (sustituye ~8 bloques duplicados de `if state.step == Step.ESCALATE and not state.pending_note: ...` repartidos por `route_message`). Cuando se envía un link directamente, se sigue generando la nota interna en Chatwoot pero la conversación NO se marca `pending` (no hay handoff real). `finalize_chatwoot_delivery` (`chatwoot.py`) ya trataba `pending_note` y `pending_escalation_reason` como señales independientes — solo faltaba una vía para setear la primera sin la segunda.
  - **Botón "💵 Pagar en persona"**: nueva opción en `mixed_final_summary_actions` (carrito, value `"3"`) y en `itinerary_offer`/`summary` (servicio único, value `"cash"`, también reconoce texto libre "efectivo"/"pago presencial") para clientes de cualquier nacionalidad que no quieren pagar online. Siempre escala al asesor con un motivo dedicado ("...quiere pagar en persona, no online"), sin mencionar ningún link. Nuevo `_single_service_cash_payment_response` en `decision_tree.py`.
  - **Fix de navegación "volver" en flujo de texto libre**: `MIXED_ASK_CERTIFICATION`, `MIXED_ASK_CERT_COUNT` y `MIXED_ASK_BEGINNER_ACTIVITY` — los 3 steps del split de certificación mixta alcanzables SOLO por texto libre (el IntentDetector salta directo a ellos, nunca por clic de botón desde una pantalla anterior) — faltaban en `MENU_STEPS`/`_MIXED_FLOW_STEPS`/`BACK_STEP` de `supervisor.py`. Esto hacía que "volver" (tecleado o por botón) reseteara todo a `MAIN_MENU` perdiendo el contexto del carrito, en vez de retroceder un paso; dos de los tres steps ni siquiera mostraban el botón "🔙 Volver". Registrados los 3 steps igual que el resto de la familia `MIXED_*`, añadidos los botones de Volver que faltaban, y corregido `_ask_certification_message` (no restauraba `state.step`, causando que el siguiente clic se interpretara mal).
  - Suite: 653 passed, 1 skipped, 1 xfailed (8 tests nuevos, 5 actualizados por los nuevos botones).
- **2026-06-21 (v0.17.7)**: mergeado `origin/feature/dev_alvaro` (fast-forward, Capas 1+2) dentro de `feature/dev_gadea`, luego: (1) Capa 3 de tolerancia a typos completada; (2) primera ronda de respuestas del owner sobre huecos de negocio implementada en KB + bot. Resumen:
  - **Capa 3** (`src/agents/supervisor.py` + `src/utils/fuzzy.py`): fuzzy por palabra en `_match_quick_reply_text` (nuevo `word_ratio()`, cutoff 0.80) para botones de menú sin coincidencia exacta ("snorlkel"→Snorkel). Umbral de `DetectedIntent.confidence` ahora enruta: `>=0.30` aplica directo (lógica extraída a `_route_detected_intent`); `0.2–0.3` con actividad concreta pregunta "¿Te refieres a X? (Sí/No)" vía `state.pending_intent_confirmation` (nuevo campo en `ConversationState`), resuelto al inicio de `route_message`. `_intent_would_route()` (predicado puro, no muta estado) evita preguntar cuando la detección de igual forma no iba a cambiar el flujo (p.ej. preguntas sobre cursos que deben ir a RAG) — sin esto, 2 tests existentes se rompían. `docs/typo-resilience-plan.md` con las 3 capas ✅.
  - **Respuestas de negocio implementadas** (ronda 1-3, documento completo de Q&A en `docs/questions_for_owner_business_kb.md`): PARCEROS eliminado por completo (era un producto obsoleto, no "0% descuento") de `discounts.json`, `decision_tree.py` (mensaje ES/EN de descuentos) y de un ejemplo few-shot en `conversations.json`; descuento de grupo 4+/5% → 5+/10% (+10% online = 20% total); equipo propio $33.000 COP/día → 5% solo si trae equipo COMPLETO; nuevas FAQs de medios de pago (colombianos 50% online+50% presencial, extranjeros 100%), moneda (COP/USD), pago fallido, y matiz de que el pago online se concilia manualmente después en Excel (no es confirmación instantánea); comida propia permitida (alergias/dietas); servicio privado ya no implica solo a Andrés ("coordinamos instructor disponible"); nuevo detector `_detect_cancellation_request`/`_detect_reschedule_request` en `supervisor.py` que, ante una solicitud de cancelar/cambiar fecha de una reserva existente, muestra la política de la KB + dos botones (asesor/menú principal) en vez de escalar a ciegas o improvisar.
  - **Pendiente del owner**: el documento de preguntas tiene una ronda 4 lista para mandar por WhatsApp (no guardada en el repo, se entregó solo en el chat) — cubre descuento colombianos, precio Bubble Makers, clima/reembolsos, matriz de hoteles prioritarios, idiomas del staff, certificación PADI, combos de cursos, Divemaster, equipo técnico (tallas/lastres/máscara graduada/niños/Nitrox), extras y promos, y timing de recordatorios. Revisar `docs/questions_for_owner_business_kb.md` sección "Seguimiento" antes de la próxima ronda.
  - Suite: 644 passed, 1 skipped, 1 xfailed (sin regresiones).
- **2026-06-21 (v0.17.6)**: tolerancia a typos (Capas 1 y 2) + bugfix cantidad mixta. Resumen:
  - **Capa 1** (`src/utils/fuzzy.py` nuevo): `is_back/is_affirmative/is_negative/is_agree/is_none_selection/fuzzy_word_number` con `difflib.SequenceMatcher` (sin deps nuevas). Cubre "sii"→sí, "cancellar"→cancelar, "cuatr"→4. Reemplaza 23 comprobaciones hardcoded en `decision_tree.py` y los yes/no en `supervisor.py`. 152 tests en `tests/test_fuzzy.py`.
  - **Capa 2** (`src/agents/intent_detector.py`): patrones de actividad con tolerancia a typos — `buce\w{0,5}` (bucereo), `bauti[sz]\w{0,3}` (bautizo), `e?snork\w{1,6}`+`e?snorqu\w{0,6}` (esnorkel/snorkle/snorquel), `mini[\s-]?curso`, `no sé bucear`, `nunca h[ae] buceado`, `submarinismo`. 33 tests en `tests/test_intent_typo_tolerance.py`.
  - **Bugfix `MIXED_ADD_QTY`**: "somos cuatr personas" caía al orquestador LLM (que re-mostraba la selección de plan) porque el bloque MENU_STEPS no hacía return para texto libre que no matchea botones. Fix doble: passthrough explícito en `supervisor.py` para `MIXED_ADD_QTY`/`MIXED_CERT_REFRESH_QTY`, y fuzzy por token en `_parse_mixed_quantity`. Test de regresión añadido.
  - **Refresher split bugs** (sesión anterior): review mostraba servicio incorrecto, solo añadía subgrupo parcial, sub-bullet del refresh iba bajo el primer cert item. Todo corregido.
  - `services.json`: nombres de servicios de 1 día actualizados con "(1 día)".
  - `docs/typo-resilience-plan.md`: plan vivo de 3 capas. Capas 1 y 2 ✅. **Capa 3 pendiente**: fuzzy por palabra en `_match_text_to_button` + uso del umbral `confidence` en `supervisor.py` (ambos en `src/agents/supervisor.py`, ~0.5 días).
  - Suite: **644 passed**, 1 skipped, 1 xfailed.
- **2026-06-20 (v0.17.5)**: sesión larga de pulido del flujo de texto libre y RAG tras feedback real de conversaciones de prueba. Resumen (detalle completo en `docs/HISTORY.md`):
  - Detección de idioma en el primer mensaje (heurística de stopwords + fallback LLM en `src/agents/language_detector.py`, nuevo módulo) — ya no se repregunta el idioma si el mensaje ya lo revela.
  - Precio total de grupo en la tarjeta de previsualización (antes de confirmar "Añadir al carrito") cuando la cantidad ya se conoce; negrita solo en "(10% off)" y en los montos finales.
  - Preguntas informativas a mitad de flujo (disponibilidad, "incluye comida?", "llevar equipo?") ya no se cuelan en el orchestrator como acciones de carrito ni se quedan sin contexto — respuesta directa + botones "Continuar con la reserva"/"Inicio".
  - `_build_extra_context` (`supervisor.py`) ampliado: grupo, duración, regla de edades de niños, lancha privada, e "incluye/no incluye" real del servicio activo (previsualización, plan de certificación en curso, o items del carrito) — fuente de verdad en vez de depender de la búsqueda vectorial. `rag_agent._build_grounding_context` ahora también incluye los mensajes previos del propio bot como contexto de fundamentación (evita rechazar respuestas que confirman algo que el bot ya dijo).
  - Bug de hotel/isla: elegir el botón genérico "Ya estoy en las islas" (sin isla detectada por texto) se saltaba la pregunta de hotel en 4 puntos distintos del código — todos corregidos, incluido el cambio de origen a mitad de carrito (nuevo flag `mixed_pending_location_change` en `ConversationState`).
  - `IntentDetector`: tolerancia a typos en "certificado" (stem `cert\w*`), inferencia de actividad=buceo cuando se menciona certificación sin decir "buceo", y resolución de `group_allocation` cuando solo se da el conteo de NO certificados ("somos 2 y uno no está certificado").
  - Bug de doble-añadido: el minicurso de un grupo "algunos certificados, otros no" se duplicaba (qty×2) porque `_after_location_set()` lo auto-añadía otra vez encima de la cola `mixed_pending_beginner_after_cert`.
  - Nuevo `Step.MIXED_ASK_BEGINNER_ACTIVITY`: el no certificado de un grupo mixto ya no asume minicurso — se le ofrece Minicurso/Snorkel/(Open Water si el plan certificado ya implica dormir en la isla).
  - KB: "Seguro" añadido al snorkel de Cartagena (faltaba vs. la variante ya-en-isla) + FAQs nuevas para seguro incluido y "¿qué es Bubble Makers?". **Pendiente**: reindexar (`$env:ENV_FILE=".env.dev"; python -m scripts.load_embeddings`) para que RAG sirva estos cambios de KB.
  - Suite: 454 passed, 1 skipped, 1 xfailed.
- **2026-06-20 (v0.17.4)**: suite de tests TERMINADA tras el refactor de texto libre — de 100 skipped a 1 (solo queda el test e2e que necesita credenciales OpenAI). 418 passed. Se reescribieron 8 tests transversales (keywords escalación, navegación menu/volver, free-text→RAG, post-summary, nota de asesor) con setup directo de estado; se borraron ~61 journeys del menú guiado en `test_conversations.py` + 3 clases legacy en `test_decision_tree.py` (Steps borrados), el hook `tests/conftest.py` y los helpers rotos `reach_group_type`/`reach_diving_experience`/etc. Cobertura del flujo nuevo: `tests/FreeText/`, `test_orchestrator.py`, `test_intent_detector.py`. OJO UX: preguntas informativas que mencionan "buceo" (ej. "¿paquete sin buceo nocturno?") ahora entran al flujo de reserva (el IntentDetector capta "buceo") — revisar si algunas deberían responderse por RAG.
- **2026-06-20 (v0.17.3)**: (1) Regresión real arreglada — preguntas de buceo adaptado / DIVE TO HEAL (discapacidad, silla de ruedas, síndrome de Down) iban al flujo de reserva; ahora van a RAG vía `_ADAPTIVE_DIVING_PATTERN` en `supervisor.py` (justo tras el guard de escalación sensible). (2) IntentDetector extrae "N con/tenemos open water y M no" → `group_allocation`, y el supervisor encola `mixed_pending_beginner_after_cert` para saltarse la pregunta de certificación. (3) Triaje de tests legacy: 2 destapados (buceo adaptado), 9 transversales (routing RAG / keywords escalación) siguen skipeados porque su SETUP usa Steps borrados (GROUP_TYPE) — hay que reescribir su navegación al flujo nuevo, no la aserción. **PENDIENTE para el equipo**: reescribir esos 9 + el resto de los 98 skipped contra el flujo de texto libre (lista en `tests/conftest.py`).
- **2026-06-20 (v0.17.2)**: arreglado el flujo de grupo con certificación mixta ("algunos sí, otros no") en `decision_tree.py`. Antes metía a todo el grupo como certificados; ahora pregunta cuántos certificados (`MIXED_ASK_CERT_COUNT`), hace el subgrupo cert y luego dispara el minicurso para los no certificados (`_maybe_start_pending_beginner`, campo `mixed_pending_beginner_after_cert`). El intercept está en `_handle_mixed_add_preview` (todos los commits de cert convergen ahí). PENDIENTE/mejora futura: que el `IntentDetector` extraiga directamente "N con open water y M no" → `group_allocation` para saltarse la pregunta. Tests: `TestMixedCertificationSplit` en `test_decision_tree.py`.
- **2026-06-20 (v0.17.1)**: tras mergear el refactor de texto libre de Gonzalo (v0.17.0: IntentDetector + orchestrator + gpt-4o), la suite quedó con ~100 tests legacy en rojo (verificaban el menú guiado viejo que se eliminó). Se arregló: (1) bug de SEGURIDAD — la escalación sensible (médica/clima/quejas) y broken-link ahora corren ANTES del IntentDetector en `supervisor.py` (antes "estoy embarazada, puedo bucear" se iba al carrito en vez de escalar); (2) error de colección de `tests/FreeText/`; (3) ~100 tests legacy SKIPEADOS (no borrados) — 28 clases en `test_decision_tree.py` con `@_LEGACY_GUIDED_FLOW`, y 71 funciones de `test_conversations.py` vía hook centralizado en `tests/conftest.py` (`LEGACY_GUIDED_FLOW_TESTS`). Para reescribir un test contra el flujo nuevo: quítalo de esa lista (o del decorador) y adáptalo. OJO: algunos skipeados (routing RAG, keywords de escalación) pueden esconder regresiones reales — revisar al reescribir. Suite: 400 passed, 100 skipped.
- **2026-06-05**: `feature/dev_alvaro` fue rebaseado sobre `feature/pruebaGon` (tip `e1ee6b6`) replicando el árbol completo como un único commit de port. Esto soluciona el problema de "historias no relacionadas" que estaba bloqueando los merges con Gonzalo/Gadea. Backup en `backup/dev_alvaro_pre_pruebaGon_rebase_2026-06-05`. El push a `origin/feature/dev_alvaro` requiere `--force-with-lease` porque la historia cambió. Después de pushear, Gonzalo y Gadea pueden hacer `git fetch && git merge origin/feature/dev_alvaro` con fast-forward limpio.
- Always start with `git status --short --branch` and check whether the branch is ahead/behind the remote.
- Use `/start-context` at the beginning of a session and `/close-work` before committing/pushing.
- `/close-work` must review this file and update it whenever architecture, workflow, risks, validation, current product context, environment details, or next-session priorities changed.
- Do not mix unrelated work in the same commit; keep decision-tree, RAG, infrastructure, and data-cleanup changes separated when possible.

## Architecture snapshot

1. Chatwoot sends webhooks to `/webhooks/chatwoot`.
2. `src/channels/chatwoot.py` extracts user input, loads/saves `ConversationState` via `src/state_store.py` (Redis-backed, see below), routes through the supervisor, and sends replies back to Chatwoot.
3. `src/agents/supervisor.py` routes between deterministic decision tree, RAG, and escalation.
4. `src/flows/decision_tree.py` owns menu states, quick replies, booking summaries, and deterministic flows; it loads service details from `data/knowledge_base/services.json`.
5. `src/agents/rag_agent.py` answers free text using sanitized knowledge-base content through `src/knowledge/vector_store.py`.

- Real Chatwoot `input_select` buttons replaced numeric menu text in the decision tree.
- Button clicks are processed by reading `submitted_values`; a poller exists because local Chatwoot may not emit incoming webhooks for clicks.
- Polling also handles missed incoming messages, with deduplication to prevent double replies.
- Gadea expanded the decision tree, knowledge base, WhatsApp import tooling, retrieval/vector-store logic, and privacy handling.
- RAG was reconciled to preserve PII handling and previous history-aware retrieval/fallback behavior.
- Decision tree pricing (`PRICING_MENU`) and logistics (`LOGISTICS_MENU` + `ISLAND_MENU` / `ISLAND_HOTEL_MENU`) menus were refined based on real conversations: clearer options for salidas desde Cartagena vs. clientes ya en las islas, paquetes 5/7/9 buceos (incluido ahora 9 buceos (islas) en el bloque de precios), y submenús de logística (punto de encuentro/horarios, alojamiento/recogida, qué incluye/no incluye y qué llevar). `docs/arbol_opciones_es.md` y `TODO.md` se actualizaron para reflejar estos cambios.
- `services.json` is now the source of truth for service names, prices, inclusions, requirements, itineraries, and booking links used by the decision tree. The tree maps base services to `*_already_on_island` variants when the user is already in the islands and now exposes PADI specialties in the guided course menu.
- Raw WhatsApp exports and backups are treated as sensitive and should stay ignored/untracked.
- Country flag emojis (🇨🇴, 🇺🇸) do not render on Windows — replaced throughout with 🌎/🌐 for cross-platform compatibility.
- `src/agents/lead_summary.py` builds structured private Chatwoot notes on escalation; `state.pending_note` holds the note until sent in `chatwoot.py`.
- Lead notes are sent for all escalation types: keyword (`humano`, `agente`...), sensitive (medical, weather, complaints), and tree-internal escalation.
- `chatwoot.py` now performs the real human handoff by calling `escalate_to_human()` after sending the private lead note; `pending_escalation_reason` is only cleared when Chatwoot confirms the status toggle, so failed handoffs can be retried on later activity.
- `.claude/commands/runtests.md` provides a `/runtests` skill to run the conversation dataset with block-level keyword filtering.
- `chatwoot.py` auto-assigns new conversations to the owner agent (`CHATWOOT_OWNER_AGENT_ID` in `.env`) via `POST /conversations/{id}/assignments` AND toggles them to `open` via `POST /conversations/{id}/toggle_status` so they appear in the agent inbox instead of getting stuck in Pending. Set `CHATWOOT_OWNER_AGENT_ID=0` to disable.
- `chatwoot.py` dedupe: incoming text messages now check `{conversation_id}:{message_id}:incoming` before processing so Chatwoot's `message_created` + `message_updated` pair for the same id no longer produces double replies. Button echoes are still suppressed via `conversation_pending_echo_titles`.
- `supervisor.py` routing hardening: `_matches_escalation_keyword` uses word-boundary regex to prevent "persona" false positive; `_is_substantive_free_text` strips trailing punctuation so "hey?" routes to welcome; any bare greeting mid-flow (hola, hi, buenas…) resets state to WELCOME step.
- `supervisor.py` natural-language menu navigation: `_match_quick_reply_text` compares free text against the CURRENT `state.quick_replies` (not all BUTTON_OPTIONS) and, when it confidently matches a button, feeds the button value into the decision tree. Accent-insensitive via `_strip_accents` (NFD). Question words (`cuánto/how/what`…) short-circuit to RAG to avoid hijacking real questions.
- `supervisor.py` language-intent: `_detect_language_intent` recognises "english/ingles" and "spanish/espanol/castellano" anywhere in the message; at LANGUAGE step it picks the language and advances to MAIN_MENU, mid-conversation it switches `state.language`, acknowledges in the new language and re-shows the main menu.
- Two-level main menu: after language selection the user picks 🤿 Reservar or ℹ️ Información; Reservar → tours-de-buceo/snorkel OR cursos PADI; Información → precios / reservas y pago / logística. New `Step.RESERVA_MENU`, `Step.INFO_MENU`, `Step.TOURS_LOCATION`, `Step.BEGINNER_AGE`.
- Tours booking flow is now activity-first after location: `TOURS_LOCATION` → `GROUP_TYPE` (`diving / snorkeling / mixed diving+snorkeling`). Choosing diving opens `TOURS_EXPERIENCE` (`certified / beginners / mixed certified+beginners`). Snorkeling goes directly to the snorkeling service flow.
- PADI courses are split into focused submenus: `COURSES_ADVANCED_MENU` (Advanced / Rescue + EFR / Divemaster) and `COURSES_SPECIALTIES_MENU` (Mindful Diving, Fish ID, Naturalist, Buoyancy, Nitrox). Back navigation from summaries returns to the correct originating course menu.
- Info-leaf responses (pricing/booking/logistics) append a "back to menu" hint built by `DecisionTree._back_to_menu_hint`, paired with main_menu quick replies, so users can navigate to Reservar without re-greeting.
- Reservar branch has explicit "🔙 Volver" buttons (value=`back`) on every screen (reserva_menu, tours_location, group_type, tours_experience, tours_certified incl. island variant, tours_beginner compatibility step, beginner_age, courses_menu, courses_open_water_origin, courses_open_water_time, courses_advanced_menu). Yes/no qualifier screens (certified_last_dive, certified_experience, refresher_interest) intentionally omit the back button — they are flow control, not branch choices.
- `supervisor.py` splits menu keywords: `MENU_KEYWORDS` (`menu/inicio/start/opciones`) resets to MAIN_MENU; `BACK_KEYWORDS` (`volver/back/atras/atrás/regresar`) goes ONE STEP up via the `BACK_STEP` map (`_go_back_one_step`). When the current step has no mapping (e.g. SUMMARY/FREE_TEXT) the back keyword falls back to MAIN_MENU. The fuzzy text-to-button matcher routes "back" matches through the same back handler so the button works whether the user clicks it or types its title.
- Summary follow-up now stays inside `Step.SUMMARY` via `summary_mode`, including a dedicated `back` quick reply on itinerary-offer and follow-up states, so course summaries can show the itinerary, answer follow-up questions, and still return to the right menu without resetting the conversation.
- Visible summary CTAs for normal reservable services were simplified again: `itinerary_offer` now shows only `Ver itinerario completo + link de reserva` + `Volver`; typed `reservar` / `booking` input is still supported inside `SUMMARY`, and the booking link is rendered only in the full itinerary block.
- Divemaster is now treated as `contact_only` in `services.json` and the decision tree: summary and itinerary use localized intro/overview blocks, show the website as an info link instead of an online booking CTA, and expose a `Contactar/Reservar` path that escalates directly to the manager with a single non-duplicated confirmation message.
- `Información > Actividades` was brought in line with the current booking hierarchy. After location, the user now goes through mirrored activity submenus (diving/snorkel tours, certified/beginners/mixed diving branches, PADI courses → go pro / specialties / referral), but each leaf still returns an informational card instead of entering the booking flow.
- Info detail screens (`INFO_*_DETAIL`) now use `back_step_override` too, so back navigation returns to the exact originating info submenu, including the island-only certified `4 dives` variant selector.
- `reserva_menu` is now aligned with the real handler: it acts as a clean entry into the cart-style booking flow instead of advertising legacy branches that no longer exist in the booking UX.
- Open Water now keeps the mixed-cart preview path but adds an explicit availability warning when the user says they may not have enough time; the warning distinguishes Cartagena (2 full days + 1 overnight stay) from already-on-island variants.
- Single-service → mixed-cart upgrade now supports PADI courses and exact certified packages, not just snorkel / minicourse / generic 2-dives. Exact certified service ids are preserved through companion handling and cart preload.
- Companion flow standardization: when the user says their companion will do "the same" activity, `group_context` now carries the exact `service_id` when relevant, so group cards and mixed-cart items preserve the chosen certified package instead of collapsing back to a generic plan.
- RAG hardening (v0.16.3, 2026-06-14): hybrid retrieval confidence gate `rag_agent._is_confident()` — vector hits gate on cosine ≥ `settings.rag_min_score`, lexical hits on raw `ts_rank_cd` ≥ `settings.rag_min_bm25_rank` (new config, default 0.05); docs without branch scores fall back to generic `score`. `rag_answer` evaluates confidence BEFORE parent expansion so a `:summary` parent can't inflate it. BM25 in `vector_store._bm25_search` uses `websearch_to_tsquery('simple', …)`. Shared asyncpg pool via `vector_store._get_pool()/get_pool()` (used by `_vector_search`, `_bm25_search`, `rag_agent._expand_with_parent_context`).
- RAG correctness guards in `src/agents/grounding_check.py`, called inside `rag_agent._answer_with_llm` BEFORE the LLM `is_grounded`: `currency_amounts_grounded()` rejects answers citing a price/percentage not in context (normalizes USD `.00` and COP thousands; ignores non-currency numbers); `urls_grounded()` rejects any `http(s)://`/`www.` link not in context. On miss → fallback.
- Retrieval rerank: `vector_store.subtype_boost_for_topics()` + `SUBTYPE_BY_TOPIC` add +0.08 to the matching service sub-chunk by intent (pricing→pricing, schedule→itinerary, equipment→included, certification/refresher→requirements). `TOPIC_PATTERNS` is kept identical between `load_embeddings.py` and `vector_store.py` — keep them in sync. `rag_agent` caches faqs/policies (`_load_faqs_cached`/`_load_policies_cached`); the few-shot alias map is module-level `_FEWSHOT_TOPIC_ALIASES`.
- Safe reindex: `scripts/load_embeddings.py main(force, dry_run)` confirms before `DELETE FROM kb_documents`; flags `--yes`/`--force` (skip prompt) and `--dry-run` (per-source summary, no DB/OpenAI). Current KB is **779 docs** (after v0.18.2 reindex: +18 docs from KB Q&A update). NOTE: after editing KB you must re-run `python -m scripts.load_embeddings --force` for RAG to serve the change (use `--force` to skip the interactive "yes" prompt on Windows).
- KB: added a max-depth FAQ in `faqs.json` (mini-course/discovery 12 m, Open Water 18 m, Advanced/packages 30 m, Bubble Makers 2 m). Pending: reindex so RAG serves it.
- Decision-tree coherence (verified programmatically): every `Step` has a handler except `ESCALATE`/`FREE_TEXT` (supervisor-managed); all `set_quick_replies` keys exist in `BUTTON_OPTIONS`; all `MESSAGES[...]` refs exist; all entries are bilingual es/en; `BACK_STEP` covers all back-button menus. Removed dead `info_general` config (was in MESSAGES + BUTTON_OPTIONS, never referenced).
- RAG system prompt (ES + EN) has an explicit DIVE TO HEAL exception: disability/accessibility questions about the adaptive diving program are answered with factual program info, not escalated as medical.
- `src/agents/rag_agent.py` now short-circuits food / lunch / dietary questions to a canonical answer built from curated FAQ/policy entries before similarity search, preventing hallucinated food answers.
- `load_embeddings.py` now indexes `pricing.json` fully (8 origin × section pairs × 2 langs + 2 discount_policy docs = 441 total KB documents) and includes COP prices in `services.json` embeddings.
- Mixed-group cart flow (`src/agents/intent_classifier.py` + MIXED_* steps in decision_tree): items of the same type/plan now aggregate into a single line. Modify/remove pick use dynamic emoji buttons (`1️⃣ 2 × Buceo certificado` + `🔙 Volver`) instead of free-text "respond with number". `ConversationState.mixed_entry_path` ("diving_snorkel" | "cert_beg") drives the activity menu filter (snorkel hidden in cert+beg) and a separate `mixed_entry_cert_beg` intro that does not mention snorkel.
- Mixed summary refactored: per-person + total-bold formatting (`*qty × label*` + `qty × $price p.p. = *$total*`), `$` instead of `U$`, includes shows only when there are items. Booking links are NOT in the summary anymore — they are stored in `state.mixed_booking_links` and sent only when the user clicks the `📝 Reservar` button.
- Sequential location/Colombian for tours: `_goto_location_with_costs` puts the user through LOCATION (with bilingual cost-aware prompt: "Desde Cartagena $X / Ya en las islas $Y") → COLOMBIAN → SUMMARY. If `state.location` is already set (tests, restored conversation), LOCATION is skipped and the flow goes straight to COLOMBIAN.
- New `📝 Reservar` button on `itinerary_offer` (and on `summary` follow-up after "Ver itinerario completo"). On click: escalates to advisor + sends booking link (10% off) inline — for Colombian users the link is omitted and the advisor coordinates the discount. Removed `🙏 No gracias`.
- Itinerary view split: full itinerary and follow-up prompt are sent as two separate chat messages via `MESSAGE_SPLIT` sentinel. `src/channels/chatwoot.py` detects the sentinel and dispatches two messages (quick_replies only on the last one).
- Tours certified menu: items renamed "Buceos" → "Inmersiones" and copy of the question emphasises days. Open Water origin shows price for each option.
- Beginner age question: 3 buttons now (`👶 menores de 8` → escalate snorkel only; `👦 8-10 Bubble Makers` → escalate; `🧑 todos 10+` → normal flow). Bubble Makers wording clarified to "máximo 2 metros de profundidad".
- `_handle_reserva_menu`: choice 1 (Tours) now goes directly to GROUP_TYPE — the deprecated TOURS_LOCATION step is bypassed (the location question moved to LOCATION between service selection and COLOMBIAN).
- `mixed_add_cert_plan` in the mixed cart now uses a restored two-level certified flow: first `2 inmersiones / 1 día` vs. `paquete multi-día (3 o más inmersiones)`, then a dedicated multiday submenu with the exact packages from `services.json`. The mixed cart preserves the exact certified service id/label through split handling, summary, and lead-note generation. `mixed_cart_modify_pick` y `mixed_cart_remove_pick` ya no piden número en texto.
- `MESSAGES["escalate"]` reescrito: "Te paso con un asesor del equipo de Diving Planet" (sin "humano", sin "Para esta situación específica..."). Mismo cambio aplicado en RAG fallback y broken-link complaint.
- Servicio Privado: `services.json` ahora tiene `price_note_es`/`price_note_en` bilingüe y la sección "✅ Incluye:" del summary se oculta cuando el servicio no tiene items.
- Booking/payment links (0.16.4): patrón unificado — el cliente NUNCA recibe el link de reserva/pago. Todos los puntos que antes lo mostraban (carrito mixto Reservar, acción "reservar" del árbol en `itinerary_offer` y en `follow_up`, `_format_full_itinerary`, `_info_detail_book_action` de la rama Información, y `_referral_escalation_message`) ahora escalan al asesor con un mensaje "...te envía el link de reserva". Se conserva el `web_url` (página informativa, no es link de pago) y el formulario de exoneración. El estado `mixed_booking_links` se sigue poblando pero ya no se muestra. Botón renombrado: "Ver itinerario completo + link de reserva" → "Ver itinerario completo".
- Info branch (0.16.4): `info_menu` ya no tiene botón "Volver" (era redundante con "Inicio" porque ambos van a MAIN_MENU). `info_tours_menu` e `info_packages_menu` ya no ofrecen "Grupo mixto" — solo actividades por separado (los handlers pasaron de `_parse_choice(_, 3)` a `_, 2` y se quitó la rama `choice == 3`). Los steps `INFO_MIXED_ACTIVITY_MENU` / `INFO_MIXED_CERT_BEG_MENU` y sus handlers quedan como código muerto inalcanzable (no eliminados). La rama Reservar conserva grupo mixto.
- Lead note (0.16.4) en `lead_summary.py`: (1) eliminado el bloque "💰 Resumen compartido con el cliente / 🧾 RESERVA DIVING PLANET"; (2) "💬 Últimos mensajes del cliente" filtra con `_is_free_text()` para mostrar solo texto libre (descarta números de botón `1`/`2`/`6+` y navegación `back`/`inicio`/`menu`); (3) "🎯 Servicio de interés" usa `_service_display_name(id, lang)` (resuelve id→nombre vía import diferido de `SERVICES`) y se oculta cuando hay `mixed_cart` (el carrito refleja el interés real; `selected_service` solía quedar obsoleto de navegar Información).
- Especialidades/cursos PADI y colombiano (verificado 0.16.4): las especialidades y cursos entran al carrito mixto vía `_start_mixed_course_add`; la pregunta de colombiano SÍ se hace al confirmar el carrito (`_goto_mixed_final_colombian` en `MIXED_FINAL_COLOMBIAN`), salvo que `state.is_colombian` ya estuviera respondido antes (se hereda, no se repite). La rama Información no pregunta colombiano (solo informa / escala).
- Conversation context / orchestrator (0.16.5, Fase 1 de `docs/conversation-orchestrator-plan.md`): `_build_extra_context` (`supervisor.py`) ahora añade el carrito completo (`state.mixed_cart`) y el step actual; el RAG ve `history[-12:]` (antes 6).
- Tool-calling orchestrator (0.16.6, Fase 2/3/4 del plan): nuevo `src/agents/orchestrator.py` con 9 tools de OpenAI function-calling (`set_location`, `start_booking`, `add_to_cart`, `cart_action`, `remove_item`, `set_profile`, `note_logistics`, `escalate`, `answer_question`). El dispatcher `_dispatch_orchestrator` en `supervisor.py` se ejecuta antes del clasificador legacy (`classify_menu_intent`) dentro del flujo carrito mixto (`_MIXED_FLOW_STEPS`). El texto libre ahora puede modificar el árbol directamente: "estoy en las islas" → set_location, "quita el snorkel" → remove_item, "quiero reservarlo" → checkout. Helpers públicos en `decision_tree.py` (`orchestrator_set_location`, `orchestrator_remove_activity`, `orchestrator_start_activity`, `orchestrator_add_to_cart`) reutilizan los handlers existentes. Modelo actualizado a `gpt-4o` (config default + `.env`). Tests en `tests/test_orchestrator.py` (14 tests). Ver `docs/conversation-orchestrator-plan.md` para el diseño completo.
- Intent detection Sprint 3 (0.17.0, 2026-06-18): detección mejorada de isla/hotel + pregunta inteligente de certificación. `intent_detector.py` ahora detecta 28 hoteles con variantes/aliases (Pao Pao, Cocoliso, San Pedro de Majagua, etc.) y 12 islas (Isla Grande, Isla Marina, Isla del Pirata, etc.) con mapeo automático hotel→isla. Cuando el usuario dice "quiero bucear" sin especificar certificación, el bot pregunta explícitamente "¿Eres buzo certificado?" con botones [Sí/No] (nuevo step `MIXED_ASK_CERTIFICATION` en `decision_tree.py`). Cuando detecta isla pero NO hotel, muestra lista de hoteles de esa isla (función `_goto_island_hotel_menu()`). El resumen ahora muestra isla específica ("📍 Salida: Isla Grande") en lugar de genérico ("Islas del Rosario"). Fix crítico: `_mixed_preview_state()` ahora preserva `island` y `hotel` para que aparezcan en el resumen. Documentación reorganizada en `docs/FreeText/` con nuevo `SPRINT3_LOCATION_HOTEL_DETECTION.md`. Tests movidos a `tests/FreeText/`: `test_diving_certification_flow.py`, `test_island_hotel_flow.py`, `test_hotel_detection.py` (89 casos, 100% pasando). Ver `docs/FreeText/SPRINT3_LOCATION_HOTEL_DETECTION.md` y `docs/FreeText/STATUS_DETECCION_INTENCION.md` para detalles completos.
- RAG follow-up detection (0.16.5): `rag_agent._looks_like_follow_up` decide si una query lleva historial al retrieval. Devuelve True para fragmentos `<4` palabras, prefijos conectores ("y...", "and..."), afirmaciones de ubicación ("en el/la...", "estoy en...", "at the...") y anáforas (these/ese/mismo...). Las preguntas autosuficientes (incl. interrogativas "¿en qué...?") devuelven False y se recuperan limpias. El prompt de `query_rewriter.py` maneja respuestas cortas además de preguntas.
- RAG prompt construction (0.16.2): `SYSTEM_PROMPT_ES`/`EN` constantes desaparecidas; ahora `rag_agent.build_system_prompt(lang, query=None)` compone el prompt dinámicamente. Estructura: `_INTRO_{lang}` (frase de presentación) + bloque "Estilo y tono" generado por `_build_tone_section(lang)` desde `brand_tone.json` + `_SYSTEM_PROMPT_{lang}_BODY` (reglas estrictas + monedas + extra_context + escalación, hardcoded). Si se pasa `query`, se añade al final un bloque "Situaciones reales del centro" con 1-2 ejemplos elegidos por `_select_fewshot_examples` desde `conversations.json`.
- RAG dynamic JSON inputs (0.16.2): `brand_tone.json` y `conversations.json` se cargan vía `load_brand_tone()` y `load_conversations()` (nuevos helpers en `src/knowledge/loader.py`), cacheados en `_BRAND_TONE_CACHE` y `_CONVERSATIONS_CACHE` (lazy-load al primer uso, monkeypatcheable en tests). Cambios en estos JSON se reflejan en el prompt al reiniciar el bot — sin tocar Python.
- RAG few-shot (0.16.2): la selección usa `detect_query_topics(query)` (ya existente en `vector_store.py:43`) y un alias map ES→TOPIC_PATTERNS labels (`precios→pricing`, `disponibilidad_ultima_hora→availability`, etc.) para mapear los `extracted_topics` de `conversations.json` (en español snake_case) a los labels de `TOPIC_PATTERNS`. Filtro por `lang` y truncado a 220 chars por ejemplo (`FEWSHOT_MAX_CHARS`).
- RAG prompt dedup (0.16.2): el prompt ES/EN tenía "Gestión de precios/monedas/pagos:" y "Uso del contexto (extra_context):" duplicadas (copy-paste antiguo). Quitadas — cada sección aparece exactamente una vez. Tests de regresión: `test_system_prompt_has_no_duplicated_currency_section_es/_en`.
- Kids inline (0.16.0): la pregunta `MIXED_FINAL_KIDS` (`<8` / `8-10` / `10+` / `Varios rangos`) se dispara INLINE al añadir Minicurso al carrito mixto, no al final del checkout. `_continue_after_kids` rutea: MODIFY → cart_review, ADD → preview, legacy → private/summary. `_pending_beginner_qty()` resuelve el qty en ambos contextos (pending para add, item.qty para modify). Borrar el beginner item invalida `kids_*` counters; añadirlo de nuevo dispara la secuencia fresca.
- Cambiar origen desde carrito (0.16.0): `mixed_cart_actions` ahora tiene 6 botones (Añadir/Modificar/Quitar/Confirmar/`📍 Cambiar origen`/Empezar). `Step.MIXED_CART_LOCATION` re-pregunta Cartagena/Islas; `_remap_cart_for_location` itera el cart y cambia la `plan` field (variantes `*_already_on_island`) y los labels para `cert`/`course` items.
- Large-group kids qty (0.16.0): `_kids_qty_quick_replies` y `_kids_mixed_qty_quick_replies` ahora muestran `6+` cuando el cap es > 9 (o > 8 para la variante mixed), y los handlers usan `mixed_pending_exact` flag para pedir el número exacto en un segundo paso (mismo patrón que `MIXED_ADD_QTY`).
- Back-routing (0.16.0): `supervisor.py` tiene DOS rutas de back (literal keyword `volver/back/atras` ~línea 2548; LLM intent classifier ~línea 2694). Ambas deben listar los steps que tienen `Volver` button propio (`MIXED_CART_LOCATION`, `MIXED_CART_MODIFY_PICK`, `MIXED_CART_REMOVE_PICK`, `MIXED_FINAL_KIDS_U8`, `MIXED_FINAL_KIDS_810`) para que el back vaya por el handler (que llama `_goto_mixed_cart_review` con cart_lines) en vez de `_go_back_one_step` (que solo retorna el prompt sin cart).
- Lead summary (0.16.0): `src/agents/lead_summary.py` usa `kids_under_8_count` y `kids_eight_to_ten_count` para mostrar dos líneas independientes en el grupo mixto. Fallback legacy para estados sin counters (mira `kids_age_group`).

## Current product context

- The bot supports Spanish/English decision-tree flows and free-text RAG answers.
- Chatwoot buttons are sent as `input_select` quick replies, while numeric/text fallback still works.
- Local Chatwoot may not always emit incoming webhooks for button clicks, so the bot includes polling/deduplication logic.
- The decision tree has recently been improved for:
  - Cartagena certified 2 dives / 1 day.
  - Summary flow: initial summary is short and offers an optional full itinerary; the itinerary offer is handled in `Step.SUMMARY` with `summary_mode`, supports `back`, and only then transitions to `FREE_TEXT` when the user chooses to ask more. The visible CTA for regular services is now itinerary-only (`Ver itinerario completo + link de reserva` + `Volver`), while typed `reservar` still works. 3 buceos (islas) pasó a "core split" (pide última inmersión y nacionalidad antes del resumen), igual que 2/5/7/9.
  - Certified package standardization: Cartagena `3 dives` is again `1 day` (2 daytime + 1 night dive), and all certified multi-day/night-dive packages now show explicit island-accommodation requirements in menus, summaries, and itinerary/detail views.
  - Mixed cart certified booking: the user again sees a short top-level certified menu (`2 dives / 1 day` vs. `multi-day package`) and a second-level submenu with all restored multi-day packages from `services.json`, including island variants.
  - Booking-flow consistency: `Reservar` entry copy, Open Water time warning, mixed-flow back labels, and single-to-mixed companion upgrades were standardized and regression-tested together.
  - Companion + mixed-cart exact-plan preservation: exact certified packages now survive "same activity" handling both from single-service summaries and from inside mixed cart when a single exact cert item is already loaded.
  - Certified summary `ℹ️` blocks for Cartagena/island packages were shortened so they emphasize only that hotel/accommodation is not included, instead of repeating long descriptive text.
  - Tours branch restructure: after location the user chooses diving / snorkeling / mixed; snorkeling is direct and diving has its own certified/beginners/mixed submenu.
  - Cartagena diving beginners: `Only beginners` now goes directly to the minicourse age question (no private-service option in that branch).
  - Cartagena certified multi-day packages: 5/7/9 dives, lodging/nocturnal notes, and refresher handling.
  - (v0.19.30) Natural-language dive-count AND day-count ("5 inmersiones" / "un paquete de 3 días") now resolve the exact multi-day plan deterministically wherever the cert-plan flow is reached (upfront, after location, after hotel, after cert-count split, mixed-group), with short 2-option sub-menus for the genuinely ambiguous cases (4 dives on-island; 1/2-day packages). See `docs/HISTORY.md` v0.19.30 and `tests/test_cert_multiday_matrix.py`.
  - Island-based certified and beginner service variants from `services.json`.
  - PADI advanced/professional courses and specialties, now separated into Go Pro vs. Specialties submenus.
  - `Información > Actividades` now mirrors the updated booking hierarchy, including correct back-navigation from info detail cards and the island `4 dives` variant path.
  - Divemaster contact-only selling flow with richer summary/itinerary copy and manager handoff CTA.
  - Food-related free-text answers now come from canonical KB copy before retrieval, so lunch/dietary responses stay deterministic.
- `faqs.json` has been expanded with curated educational diving content for beginners, safety, equipment, course comparisons, underwater sensations, marine-life etiquette, and Rosario Islands destination knowledge.
- Current MVP direction: inform, qualify, recommend, and prepare human-assisted conversion; do not automate live availability, payment, or final booking confirmation yet.
- Conversation-state migration from in-memory storage to Redis is **done** (v0.19.0, `src/state_store.py`). PRE is deployed and live at the temporary domain (see Environment details below).
- Use `docs/mvp-intent-matrix.md` and `docs/kb-audit-mvp.md` before expanding tree/RAG behavior.
- `docs/infra-simple.excalidraw` contains the current minimal infrastructure scheme for team communication.
- Mixed groups, private services, and logistics constraints by hotel/island are still areas for systematic polishing. **Cancellation/change rules**: bot UX behavior is now implemented (policy text + advisor/home buttons), but the exact penalty deadlines/no-show policy text itself is still a KB gap pending owner confirmation (see `docs/questions_for_owner_business_kb.md` #11).
- COP pricing is now in the KB; bot needs a restart in WSL2 to serve it after the re-index run. Embeddings reindex done (445 docs) para incluir nuevos servicios y ajustes de precios.
- `CHATWOOT_OWNER_AGENT_ID=1` should be added to `.env` (owner agent ID confirmed via `/api/v1/profile`).
- Next session priorities:
  - **🟡 Decisión pendiente del equipo — atajos canónicos RAG: ¿lista de exclusión o de permitidos? (2026-07-16)**: `_canonical_price_overview_answer` (`rag_agent.py`) usa una lista de exclusión (deny-list) para decidir si una pregunta de precio es genérica; cualquier expresión regional/sinónimo no cubierto dispara igual el resumen genérico en silencio (causa raíz del bug de "paquetes multidía"). Ya se añadió una red de seguridad (frase de invitación a precisar) en las 3 funciones que devuelven texto fijo (`_canonical_food_answer`, `_canonical_diving_overview_answer`, `_canonical_price_overview_answer`) y se endureció el logging (`[RAG][CANONICAL_SHORTCUT]`, query completa). Falta decidir si migrar a lista de permitidos (safe-list) — ver comparativa completa con pros/contras y ejemplos en `docs/canonical-shortcuts-safelist-decision.md`.
  - **🟡 Verificar en vivo en PRE — FAQ nueva de "2 inmersiones desde Cartagena" (v0.20.7)**: añadida en `faqs.json` para arreglar el bug de RAG (precio equivocado / alucinación de "no existe el paquete"), pero no se pudo probar localmente (sin `OPENAI_API_KEY`/Postgres+pgvector). Tras el deploy+reindex a PRE, probar "¿cuánto cuestan 2 inmersiones?" y confirmar que da $178/$197.29 (no $287) y no niega que exista el paquete.
  - ~~**🟡 Gap sin resolver — plan pendiente no se remapea al cambiar ubicación a mitad de flujo (v0.19.30)**~~ ✅ **CERRADO en v0.19.31** — `_remap_cart_for_location` ahora también remapea `state.mixed_pending_qty_plan`; `orchestrator_set_location` ya no se salta el remap con el carrito vacío.
  - ~~**🟡 Gap sin resolver — detector de splits de grupo mixto frágil al orden de palabras (v0.19.30)**~~ ✅ **CERRADO en v0.19.31** — infijo opcional de conteo intercalado en `pat_numeric_fwd`/`pat_numeric_rev` (`intent_detector.py`).
  - ~~**🔴 Bug sin resolver — acompañante inventado (T113)**~~ ✅ **CERRADO en v0.19.22** — ver entrada de arriba y detalle en `docs/test-battery-edge-cases.md`.
  - **Reindex pendiente en PRE/PRO**: varios fixes de KB de esta y sesiones anteriores (v0.19.17/18/19/22) solo se aplicaron en dev. Correr `python -m scripts.load_embeddings --force` con el `.env` de cada entorno antes de que el owner pruebe en PRE.
  - **Batería de tests ✅ COMPLETA (v0.19.27)**: `docs/test-battery-edge-cases.md` — las 176 casillas de las 23 categorías están tachadas tras la pasada de reconfirmación formal. De los 2 riesgos de flakiness no determinista documentados (registro "cont. 11"): **(1) ✅ MITIGADO en v0.19.28** — el falso-fallback intermitente (~1/3) del verificador de grounding: `_answer_with_llm` ahora regenera la respuesta una vez ante cualquier rechazo de guard (2 intentos), así un muestreo puntual desafortunado a temp=0.3 ya no cae directo al fallback si existe contexto válido; solo cae si ambos intentos fallan. **(2) ✅ MITIGADO en v0.19.29** — la capacidad inventada del tour privado: nuevo guard determinista `capacity_claims_grounded()` (`grounding_check.py`) rechaza afirmaciones de capacidad máxima de personas cuyo número no esté en el contexto, análogo a los guards de precios/URLs. Ambos riesgos de flakiness quedan mitigados de forma determinista.
  - ~~**Siguiente paso acordado (técnico):** migrar el estado conversacional de memoria a Redis/PostgreSQL.~~ ✅ HECHO (v0.19.0) — `src/state_store.py`, PRE ya desplegado y probado en vivo.
  - **Siguiente prioridad real:** darle el link `https://pre.is-core.dev/chat` al owner para que empiece a probar de verdad. En paralelo: (1) pedir/gestionar acceso a HostGator para migrar del dominio temporal `is-core.dev` a `pre.divingplanet.org`, (2) Booking Agent (sigue siendo un stub), (3) política de cancelación/pagos (pendiente del owner), (4) solicitar WhatsApp Business API (reloj externo, pedir cuanto antes).
  - **Matriz de hoteles (bloqueado en el equipo):** el owner entregó la matriz completa (`Dudas_V2.docx`, ver `TODO.md` sección "Matriz hotel→recogida"). NO empezar hasta que el equipo confirme cuáles de los 8 hoteles "posibles obsoletos" (incl. Pao Pao) siguen vivos. Luego: montar `data/knowledge_base/hoteles.json` (nombre→categoría) como fuente única y reindexar.
  - **Live E2E ✅ HECHO (v0.18.3):** los 20 escenarios que estaban pendientes de probar en Chatwoot real se validaron todos el 2026-07-02 (ver `readiness-audit-2026-07-02.html`). Los ítems "Live E2E retest…" de abajo quedan cubiertos por esa pasada salvo que se toque el código relacionado.
  - **RAG calidad**: ~~`en_followup_islands`~~ ✅ resuelto (fix 2026-07-01, confirmado 2026-07-02 — baseline 39/39). No quedan fallos cualitativos RAG pendientes en la baseline actual. Ver `docs/rag-prioridades-baseline.md` para detalle.
  - **Live E2E retests requeridos (v0.18.1)** — requieren Chatwoot widget activo:
    - Preguntar "¿cuánto en pesos colombianos?" → RAG debe responder con precios COP de 2 buceos y minicurso (ya indexados).
    - Preguntar "¿me recogen en Pao Pao?" → RAG debe expresar incertidumbre sobre el muelle (no sobreafirmar acceso marítimo).
    - Flujo certified: seleccionar servicio → pricing → volver al flujo → confirmar que la pregunta "¿eres colombiano/a?" NO se repite.
    - Preguntar "¿cuánto en euros?" → RAG debe explicar que no manejamos euros y que el banco convierte desde USD.
    - "Ver itinerario completo" → debe llegar como DOS mensajes separados en Chatwoot (MESSAGE_SPLIT sentinel).
    - Cursos PADI Advanced/Specialties/Referral desde Cartagena → debe aparecer la pregunta de ubicación antes de añadir al carrito.
  - **Live E2E retest del link de reserva directo (v0.17.8)**: confirmar carrito mixto con 2+ actividades como cliente NO colombiano → "📝 Reservar / contactar asesor" devuelve los links sin escalar. Repetir como colombiano → escala. Repetir con servicio único.
  - **Live E2E retest del botón "💵 Pagar en persona" (v0.17.8)**: en resumen mixto y servicio único, confirmar que aparece y siempre escala sin mencionar link.
  - **Live E2E retest de "volver" en split de certificación mixta (v0.17.8)**: "quiero bucear" → "volver" en cada pantalla → un paso atrás, nunca reset a Inicio.
  - **Ronda 4 Q&A owner — PARCIALMENTE implementada (v0.18.2)**: implementados en KB: precio Bubble Makers, política clima (reschedule first), idiomas staff (ES/EN), eCard PADI, combos cursos, máscaras graduadas, equipo niños, Nitrox bajo pedido, group_discount excluye PADI courses. **Pendientes aún**: descuento colombianos (owner no ha confirmado), matriz hoteles prioritarios, Divemaster (precio/estructura), extras/promos, timing recordatorios, cancellation/payment deadlines.
  - ~~**MIXED_FINAL_KIDS / MIXED_FINAL_PRIVATE "stuck" bugs**: state-loss en server restart.~~ ✅ Resuelto (v0.19.0) por la migración a Redis — el estado ahora sobrevive a un reinicio del proceso.
  - **RAG técnico backlog**: recalibrar BM25 rank (`rag_min_bm25_rank`), source weights, semantic cache.
  - Live E2E retest del nuevo flujo de cancelación/cambio de fecha: "quiero cancelar mi reserva" → botones 🧑‍💬 Asesor / 🏠 Inicio → ambos navegan correctamente.
  - Live E2E retest de la confirmación de baja confianza (Capa 3): mensaje con typo ("esnorkel") → "¿Te refieres a snorkel? (Sí/No)".
  - Live E2E retest of certified dive package summaries in Chatwoot (Cartagena `3 dives (1 day)` and island variants).
  - Live E2E retest of PADI course menus (Go Pro vs. Specialties, back from summary/itinerary).
  - Live E2E retest of Divemaster contact-only flow.
  - Cancellation/payment policy KB completion (deadlines y penalidades, pendiente confirmación del owner).
  - ~~PRE deployment checklist.~~ ✅ HECHO (v0.19.0) — ver Environment details.

## Knowledge base and privacy

- Versioned KB lives under `data/knowledge_base/` and must be curated/sanitized.
- Important KB files: `services.json`, `faqs.json`, `policies.json`, `pricing.json`, `availability.json`, `conversations.json`, `brand_tone.json`.
- Treat `faqs.json` as curated public-facing content: keep new entries bilingual, consolidated by topic, and avoid adding medical clearance, live availability, unsanitized customer details, or unverified booking/payment claims.
- Never stage raw exports or private customer data:
  - `wasap/`
  - raw `_chat.txt` exports
  - voice/photo/sticker/contact/PDF exports
  - `data/knowledge_base/*.bak`
  - `data/knowledge_base/*pre_dedup*`
  - real private payment links, IDs, phone numbers, emails, documents, or customer-identifying content unless sanitized.

## Standard validation

Run after meaningful code changes:

```powershell
python -m pytest tests/test_chatwoot_buttons.py tests/test_decision_tree.py tests/test_rag_safety.py
python -m compileall src tests
```

If retrieval, embeddings, or vector-store behavior changes, also run:

```powershell
python -m pytest tests/test_retrieval_rerank.py
```

## Dev environment notes

- Bot runs in WSL2 Ubuntu on port 8000: `python -m src.main` (with conda base active).
- Chatwoot runs in Docker on port 3300: `docker compose --profile chatwoot up -d`.
- Chatwoot webhook is configured in the DB (`chatwoot_dev.webhooks`) pointing to `http://host.docker.internal:8000/webhooks/chatwoot`.
- Webhook subscriptions must include `message_created`, `message_updated`, `conversation_created`, `conversation_status_changed`. Missing `message_updated` means button clicks are ignored.
- `host.docker.internal` from Docker resolves to `192.168.65.254` (Docker Desktop host), which reaches WSL2 bot via Docker Desktop port bridging.
- `CHATWOOT_BASE_URL=http://localhost:3300` in `.env` is correct for bot→Chatwoot API calls from WSL2.
- To verify webhook config: `docker exec dp-dev-postgres psql -U postgres -d chatwoot_dev -c 'SELECT id, url, subscriptions FROM webhooks;'`
- `chatwood-test.html` is **gitignored** (contains personal Chatwoot tokens per developer). Each developer keeps their own local copy with their inbox's websiteToken; do not re-add it to the index. To get the website widget token, query the Chatwoot API: `curl -H "api_access_token: <YOUR_TOKEN>" http://localhost:3300/api/v1/accounts/1/inboxes`. The file also has SDK onerror/retry + status bar so the widget surfaces clear errors when Docker/Chatwoot is unreachable instead of failing silently.
- Refresher handling in the cart-style mixed-group flow now produces a free line item (`type: "refresh"`) that is hidden from the paid rows of `_format_mixed_final_summary` and rendered as `🧑‍🏫 Refresher incluido: N personas — sin coste adicional` inside the EXTRAS block. The mixed flow asks both about interest AND about quantity (`MIXED_CERT_REFRESH_INTEREST` → `MIXED_CERT_REFRESH_QTY`). The companion-from-single flow now mirrors this with a `mixed_from_single_refresher_qty_pending` step (asks qty for 2+ certified divers, auto=1 for single companion) and `group_context["refresher_qty"]`. `_enter_mixed_flow_from_single` adds both the speaker's refresher (when `state.refresher_interested` was set during the original 2-dives flow) and the companion refresher quantity as one combined `refresh` cart line. `REFRESHER_PRESERVE_SERVICES` now includes `2_dives_1_day` and its island variant, so accepting a refresher in the single 2-dives flow does NOT swap the service to minicourse — it stays as buceo certificado with an annotation. Cart label for refresh items is "Refresher (sin coste)".

## PRE environment (VPS)

- Live at **https://pre.is-core.dev/chat** (bot + Chatwoot widget) since 2026-07-03 (v0.19.0).
- VPS: Hetzner CX23 (2 vCPU / 4GB RAM), Falkenstein/Helsinki, IP `89.167.4.161`. SSH key: `~/.ssh/dp_pre_vps` (ed25519, no passphrase, public key registered on the Hetzner project).
- Code lives at `/opt/diving-planet-bot` on the VPS, deployed via `tar | ssh` (not `git clone` — the repo had uncommitted work at deploy time; re-sync individual files with `scp` after edits, or re-tar the whole tree, whichever is smaller).
- Domain: **`is-core.dev`** (Namecheap, bought specifically for this) is a **temporary stand-in** for `divingplanet.org` — nobody on the team currently has HostGator access to add DNS records for the real domain. `pre.is-core.dev` and `chatwoot.is-core.dev` both point to the VPS IP; HTTPS via Caddy + Let's Encrypt (`Caddyfile`, real certs, no warnings). Swap back to `pre.divingplanet.org` once HostGator access is sorted — update `Caddyfile` + `.env.pre` (`CHATWOOT_BASE_URL`) + Chatwoot's `FRONTEND_URL` in `docker-compose.vps.yml`.
- Chatwoot has its **own dedicated** Postgres/Redis (`dp-chatwoot-postgres`/`dp-chatwoot-redis` in `docker-compose.vps.yml`), independent of `dp-pre-*`/`dp-pro-*` — starting Chatwoot never touches bot data, and it doesn't block on PRO being deployed.
- Chatwoot admin account: `admin.iscore@gmail.com` (created via `rails runner` console, not the public sign-up form — `ENABLE_ACCOUNT_SIGNUP` stays `"true"` in compose for now but nothing currently links to `/app/sign_up` publicly). Account id 1, inbox id 1 ("Diving Planet Bot", Website Channel), webhook already registered pointing to `https://pre.is-core.dev/webhooks/chatwoot`.
- **Caddy + underscore-header gotcha**: Caddy drops any incoming header with an underscore in its name (e.g. `api_access_token`) before proxying — confirmed by testing the exact same request internally (works, 200) vs. through Caddy (fails, 401), and explicit `header_up` re-injection from `{http.request.header.*}` placeholders doesn't help because the header is already gone by the time Caddy's request object is built. Workaround: `settings.chatwoot_api_url` (env `CHATWOOT_API_BASE_URL=http://dp-chatwoot:3000`) routes the bot's own Chatwoot API calls over the internal Docker network, bypassing Caddy entirely. `chatwoot_base_url` (public HTTPS URL) is still used for CORS and the widget SDK embedded in `/chat`, which real browsers need. If this bites again elsewhere, same fix: don't loop container-to-container traffic through the public reverse proxy.
- Redeploy after a code change: `scp` the changed file(s) into `/opt/diving-planet-bot/...`, then `docker compose -f docker-compose.vps.yml up -d --build dp-pre-bot` (or the relevant service) over SSH.
- Secrets (`.env`, `.env.pre`) live only on the VPS, generated with `openssl rand -hex` server-side — never committed, never printed to any transcript/log. `.env` holds the Postgres/Redis/Chatwoot passwords used for docker-compose `${VAR}` substitution; `.env.pre` holds the bot's own settings (OpenAI key, Chatwoot tokens, etc.).

## Manual Chatwoot checks

When touching Chatwoot, buttons, routing, or conversation state:

- Start the bot: `python -m src.main` in WSL2 Ubuntu (conda base).
- Start Chatwoot: `docker compose --profile chatwoot up -d`.
- Open `chatwood-test.html` in a browser.
- Test `hola` → language buttons (🌎 Español / 🌐 English).
- Click `Español` → main menu buttons (🤿 Reservar / ℹ️ Información).
- Type `reservar` (text instead of clicking) → must advance to RESERVA_MENU (fuzzy text match).
- Type `in english` or `me lo puedes decir en español?` mid-conversation → must switch language and re-show main menu.
- Send free text from a menu → RAG response without duplicate replies.
- After a pricing/booking/logistics answer, confirm the back-to-menu hint appears and quick replies are main_menu.
- At any Reservar step, click `🔙 Volver` → must go ONE step up (e.g., TOURS_CERTIFIED → TOURS_EXPERIENCE), not all the way to MAIN_MENU.
- Type `volver` or `atrás` mid-Reservar → same one-step-up behaviour as the button.
- Click through at least one full booking path.

## Where to look first

- `docs/HISTORY.md`: version-level overview.
- `docs/mvp-intent-matrix.md`: commercial intent ownership across tree, RAG, and human handoff.
- `docs/kb-audit-mvp.md`: current KB coverage and gaps for MVP robustness.
- `docs/infra-simple.excalidraw`: simple current infrastructure diagram.
- `src/flows/decision_tree.py`: deterministic flow and service catalog.
- `tests/test_decision_tree.py`: expected decision-tree behavior.
- `src/agents/supervisor.py`: routing rules.
- `src/agents/rag_agent.py`: free-text answering and safety prompt.
- `src/channels/chatwoot.py`: webhook, polling, and message sending.
- `TODO.md`: current product backlog and branch-polishing checklist.
